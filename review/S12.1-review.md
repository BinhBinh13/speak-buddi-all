# Review — S12.1: Trang Privacy Policy + Terms of Service (công khai)

- **Ngày:** 2026-06-07
- **Verdict:** PASS

## Đối chiếu Acceptance Criteria

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-12.1-1 — Route công khai `/privacy` | ✅ đạt | `App.jsx:67` — `<Route path="/privacy" element={<PrivacyPolicyPage />} />` nằm ngoài `<ProtectedRoute>`; guest không bị redirect `/login`. |
| AC-12.1-2 — Route công khai `/terms` | ✅ đạt | `App.jsx:68` — tương tự, render `TermsOfServicePage` công khai. |
| AC-12.1-3 (§4.7) — Thông báo thu thập dữ liệu | ✅ đạt | `PrivacyPolicyPage.jsx` mục 2: liệt kê đủ nhóm dữ liệu + bảng phân loại Public/Internal/Confidential/Restricted theo §4.6. |
| AC-12.1-4 (§4.7) — Quyền yêu cầu xóa dữ liệu | ✅ đạt | Mục 6 nêu rõ quyền xóa, trỏ S12.2 tương lai + liên hệ qua `support@speakbuddi.vn` (placeholder có TODO). |
| AC-12.1-5 (§4.6) — Chính sách lưu trữ | ✅ đạt | Mục 4: audio phát âm 30 ngày, metadata/điểm lâu hơn, audio hội thoại AI không lưu mặc định (chỉ transcript). |
| AC-12.1-6 (§4.7) — Terms: phạm vi dịch vụ & tuân thủ v1.1.0 | ✅ đạt | `TermsOfServicePage.jsx`: mô tả dịch vụ, gói free/Pro (BR02–BR06), thanh toán ủy quyền không lưu thẻ; Privacy mục 7 + Terms mục 5 nêu phạm vi tuân thủ (chưa GDPR/SOC2/ISO27001/HIPAA đầy đủ; không PCI-DSS trực tiếp). |
| AC-12.1-7 — PublicNavbar + PublicFooter + design system | ✅ đạt | `LegalLayout.jsx` bọc `PublicNavbar`/`PublicFooter`; style qua `UI` tokens (`designTokens.js`); nội dung tiếng Việt. |
| AC-12.1-8 — Footer links | ✅ đạt | `PublicFooter.jsx:7-8` — "Điều khoản" → `/terms`, "Bảo mật" → `/privacy` qua `<Link>`. |
| AC-12.1-9 (§4.8) — Responsive & accessibility cơ bản | ✅ đạt | `LegalLayout`: padding `clamp`, prose maxWidth 48rem, bảng responsive ≤480px, heading phân cấp h1/h2/h3, link luôn có underline (không chỉ dựa màu). |
| AC-12.1-10 — Link chéo + ngày cập nhật | ✅ đạt | `LegalLayout` header hiển thị "Cập nhật lần cuối: 07/06/2026"; `crossLink` Privacy ↔ Terms ở cuối mỗi trang. |

**Bonus (tùy chọn plan):** `RegisterPage.jsx:641-655` — dòng đồng ý điều khoản + link `/terms`, `/privacy` khi đăng ký (informed consent §4.7).

## Kiểm chứng đã chạy

- **`npx eslint`** (6 file story: `PrivacyPolicyPage`, `TermsOfServicePage`, `LegalLayout`, `App.jsx`, `PublicFooter`, `RegisterPage`) → **0 lỗi**.
- **`npm run lint`** (toàn repo) → 7 lỗi có sẵn ở `RoadmapSection.jsx` / `RoadmapNode.jsx` — **ngoài phạm vi S12.1**.
- **`npx vite build`** → **fail** do `adminCrawlerService.js` import `apiClient` không tồn tại trong `client.js` — **ngoài phạm vi S12.1** (S9.3/S9.4); không liên quan file legal.
- **Đối chiếu code vs plan/implement log** — khớp: 3 file mới `features/legal/*`, sửa `App.jsx`, `PublicFooter.jsx`, `RegisterPage.jsx`; không có thay đổi BE (đúng plan).

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa
- **Email hỗ trợ & tên đơn vị là placeholder** (`support@speakbuddi.vn`, "Đội ngũ SpeakBuddi") — cần PO xác nhận trước production; đã có `// TODO` trong code.
- **Nội dung pháp lý tự soạn** theo SRS — khuyến nghị rà soát bởi bộ phận pháp chế trước go-live (ghi nhận trong plan mục 6).
- **Privacy Policy trỏ quy trình xóa dữ liệu qua email** — khi S12.2 (xóa dữ liệu in-app) và S12.3 (form liên hệ) hoàn thành, cần cập nhật đoạn văn tương ứng.

### 🟢 Quan sát thêm (ngoài phạm vi)
- Không có mockup UI riêng — layout prose căn giữa phù hợp plan.
- CMS quản lý nội dung pháp lý chưa có (hard-code FE — đủ v1.1.0).
- `vite build` fail toàn repo do lỗi import admin crawler — nên fix ở story liên quan, không thuộc S12.1.

## Kết luận

Story S12.1 **PASS**: hai trang pháp lý công khai `/privacy` và `/terms` hoàn chỉnh, bám SRS §4.6/§4.7, footer/register links đúng, responsive cơ bản, ESLint sạch trên phạm vi story. Không blocker. Nợ nhỏ: placeholder email/tên pháp lý, rà soát nội dung trước production, cập nhật Privacy khi S12.2/S12.3 xong.
