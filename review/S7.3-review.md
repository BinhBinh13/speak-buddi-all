# Review — S7.3: Paid User hội thoại không giới hạn

- **Ngày:** 2026-06-07
- **Verdict:** PASS

## Đối chiếu Acceptance Criteria
| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-09-03 / BR05 — Paid user `/speak` không check quota, không 429, không cộng `used_seconds` | ✅ đạt | `routers/ai.py:38-90` — toàn bộ quota guard (`get_or_create_active_window`, check `>= max_seconds` → 429, `add_used_seconds`) bọc trong `if not is_paid`. Paid user bỏ qua tạo window và cộng giây ở cả nhánh TTS-fail (l.80) lẫn nhánh thành công (l.88). |
| `is_paid` trong JWT | ✅ đạt | `auth/jwt.py:40-49` embed claim `is_paid` vào access token. `routers/auth.py` resolve qua `get_is_paid()`: login (l.79-82), refresh (l.102-104), me (l.112-113), google (l.139-142). Register cứng `is_paid=False` (đúng — user mới chưa có subscription). |
| `is_paid` đọc từ `user_subscription` (không phải role — BR01/BR04) | ✅ đạt | `repositories/user_repo.py:72-83` `get_is_paid()` query `user_subscription` status='active' AND (`expires_at` NULL hoặc tương lai). Nguồn sự thật đúng. |
| `GET /api/ai/quota` paid → `{is_paid:true, unlimited:true, is_exceeded:false}` | ✅ đạt | `routers/ai.py:127-136` trả đúng payload cho paid user; free user (l.138-143) đi qua `get_quota_status`. |
| FE Pro badge render khi `quota?.is_paid === true` | ✅ đạt | `ConversationPage.jsx:348-367` render pill "✨ Pro · Không giới hạn" với optional chaining, an toàn khi `quota=null`. |
| FE free user vẫn thấy QuotaBar | ✅ đạt | `ConversationPage.jsx:319-345` QuotaBar render khi `quota && !quota.is_paid`. QuotaBanner (l.492-525) chỉ set khi `err.status === 429` (l.170-173). |
| FE input không bị disable nhầm cho paid user | ✅ đạt | Input/nút gửi disable theo `loading || !!quotaBanner` (l.565, 609). `quotaBanner` chỉ set khi nhận 429 (l.170); paid user không bao giờ nhận 429 → input luôn mở. |

## Kiểm chứng đã chạy
- `npm run lint -- --max-warnings=50` → **8 errors, 1 warning**, TẤT CẢ ngoài phạm vi S7.3:
  - `RoadmapSection.jsx` (5: unused vars + conditional hook)
  - `RoadmapNode.jsx` (2: unused vars)
  - `TopicModal.jsx` (1: unused var)
  - `PronunciationPage.jsx` (1 warning: missing dep)
  - `ConversationPage.jsx` → **0 lỗi** (file duy nhất S7.3 chạm vào). Lệnh exit 1 do lỗi pre-existing ở story khác, không phải do thay đổi S7.3.
- Đọc trực tiếp BE: `ai.py`, `auth/jwt.py`, `routers/auth.py`, `user_repo.py` — khớp 100% mô tả implement log; không có thay đổi BE trong story này (đúng plan, logic đã sẵn từ S7.2).

## Phát hiện
### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa
- Không có trong phạm vi S7.3.

### 🟢 Quan sát thêm (ngoài phạm vi)
- **Token staleness:** `is_paid` đóng băng vào access token tại thời điểm cấp; nếu subscription hủy giữa lúc token còn hiệu lực, user vẫn unlimited tới khi refresh. Đã ghi nhận trong plan §6 và implement log — để EPIC-8/12 xử lý revoke/short-TTL. Không chặn S7.3.
- **Lint debt pre-existing:** 8 lỗi ở `RoadmapSection.jsx` / `RoadmapNode.jsx` / `TopicModal.jsx` / `PronunciationPage.jsx` cần dọn ở story tương ứng (EPIC-2/EPIC-6). Không thuộc S7.3.
- **Badge gradient `#f59e0b → #d97706`:** dùng `C.warning` (#f59e0b) đã khai báo trong file + một biến thể đậm hơn; DESIGN.md không định nghĩa token gold riêng nên gradient vàng-cam là lựa chọn hợp lý theo CLAUDE.md §6, không tự chế palette mới ngoài hệ. Chấp nhận được.

## Kết luận
Mọi AC đều đạt: backend đã enforce đúng AC-09-03/BR05 (paid user bypass toàn bộ quota guard, không 429, không cộng `used_seconds`), `is_paid` resolve từ `user_subscription` và embed vào JWT ở mọi điểm cấp token, `/api/ai/quota` trả `unlimited:true`. FE thêm Pro badge an toàn (optional chaining, gradient hợp design system) mà không phá QuotaBar/QuotaBanner của free user, và input không bị disable nhầm vì paid user không nhận 429. Lint của file S7.3 sạch; mọi lỗi lint còn lại là pre-existing ngoài phạm vi. Verdict **PASS** → story chuyển `done`.
