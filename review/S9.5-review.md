# Review — S9.5: Admin chỉnh/disable nội dung đã crawl + giữ lịch sử crawl

- **Ngày:** 2026-06-07
- **Verdict:** PASS

## Đối chiếu Acceptance Criteria

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-13-06 — disable/edit crawl + giữ crawl history | ✅ đạt | `admin_locked` migration; Admin DELETE/PUT set lock; `upsert_langeek_*` skip locked; crawler batch disable không ghi lock; job/log tables không bị xóa khi disable content. |
| AC-13-06 — thay đổi crawler config | ✅ đạt (verify) | `PUT /api/admin/crawler/config` + `check_crawl_permitted` đã có từ S9.3/S9.4; S9.5 không regress. |
| BR11 — disabled không hiện learner roadmap/vocab/tests | ✅ đạt | Read-side vẫn filter `is_active=TRUE`; topic inactive → `/api/topics/{id}/words` 404 dù word con active. |
| FR-17 / FR-19 — edit/disable sau auto-publish | ✅ đạt | Crawler không ghi đè khi `admin_locked=TRUE`; stats/log `*_skipped_locked` trong `crawler_publish.py`. |
| Re-enable (deferred S9.2) | ✅ đạt | `PATCH /api/admin/topics\|words\|tests/{id}/enable` + `ConfirmEnableModal` trên Topics/Vocabulary/Tests/Editor. |
| S3.4 — progress giữ khi disable/re-enable | ⚠️ chưa smoke E2E | Thiết kế không xóa `user_word_progress`; chưa chạy scenario DB thật trong session review. |

## Kiểm chứng đã chạy

- **FE `npm run lint`** → 7 errors pre-existing (`RoadmapSection.jsx`, `RoadmapNode.jsx`); **0 lỗi** trong `features/admin/**` (S9.5 scope).
- **BE `python -m py_compile`** → pass: `content_repo.py`, `quiz_repo.py`, `admin_content.py`, `crawler_publish.py`, `schemas/learning.py`.
- **Router** — 3 PATCH enable dưới `require_admin`; disable vẫn DELETE S9.2 + set `admin_locked` trên topic/word.
- **Repo** — `upsert_langeek_topic/word` return `admin_locked` skip trước UPDATE; `soft_disable_langeek_words_not_in_batch` bỏ qua row locked và dùng disable crawler (không lock).
- **FE** — badge/filter Langeek, cột Source + lock icon vocabulary, cảnh báo form edit crawl, enable flow đủ 3 loại content.

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa
- **Smoke PostgreSQL E2E chưa chạy** — cần chạy migration `schema_learning_s9_5.sql` rồi verify: disable word langeek → manual crawl fixture → word vẫn inactive + locked; edit meaning → crawl không ghi đè.
- **`apiPatch` không refresh token 401** (giống `apiDelete` S9.2) — edge case session hết hạn.
- **Filter Source vocabulary client-side + pagination** — cùng pattern nợ S9.2 (filter Draft); có thể thiếu row khi nhiều trang.

### 🟢 Quan sát thêm (ngoài phạm vi)
- Topic card chưa có icon khóa (chỉ vocabulary có) — plan không bắt buộc.
- Topic inactive + crawl sync word unlocked → word có thể `is_active=TRUE` nhưng learner vẫn không vào topic inactive — chấp nhận theo plan §6.
- Re-enable clear lock → crawler sync lại field lần crawl kế — đúng plan.
- `AdminCrawlerPage` link tới Topics/Vocabulary — optional plan, chưa làm.

## Kết luận

Story S9.5 **PASS**: cốt lõi AC-13-06/BR11 được giải quyết bằng `admin_locked`, crawler publish tôn trọng quyết định Admin, FE admin đủ nhận diện nội dung Langeek và re-enable. Không blocker code. Nợ chính: smoke test PostgreSQL sau migration (giống S9.3/S9.4).
