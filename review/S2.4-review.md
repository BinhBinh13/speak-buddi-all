# Review — S2.4: Màn hình xem roadmap theo level (kèm empty state)

- **Ngày:** 2026-06-07
- **Verdict:** PASS

## Verdict: PASS

## AC Checklist

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-04-01 | ✅ đạt | `RoadmapHeader` hiển thị `level_name` (fallback `Level {level}`) + chip `level`; `RoadmapPath` sort `nodes` theo `order_index` (`[...nodes].sort((a,b)=>a.order_index-b.order_index)`) rồi render zigzag (chẵn→trái, lẻ→phải). Layout khớp mockup. |
| AC-04-02 | ✅ đạt | `RoadmapPage` fetch `getRoadmap()` trong `useEffect` mỗi lần mount (deps `[retryCount]`), không cache cứng phía FE. Chỉ render `data.nodes` từ BE, không hardcode danh sách topic → Admin/crawler bật-tắt topic phản ánh khi vào lại trang. |
| AC-04-03 | ✅ đạt | Route `/roadmap` nằm trong `<Route element={<ProtectedRoute />}>` (App.jsx:65,92). `ProtectedRoute` chặn guest → `/login?next=`. `apiClient` xử lý 401: thử refresh token, thất bại → `clearTokens()` + redirect `/login`. Onboarding sau submit redirect `/roadmap` (OnboardingPage.jsx:123). |
| AC-04-04 | ✅ đạt | Khi `!data.nodes || nodes.length===0` → render `<RoadmapEmptyState>`, `<RoadmapHeader>` vẫn hiển thị phía trên (giữ context level). FE chỉ gọi GET `/api/roadmap`, không có mutation nào → không xóa level/progress. Empty state có CTA "Cập nhật trình độ" → `/profile`. |

## Kiểm chứng đã chạy

- `cd speak-buddi && npm run lint` → 10 errors + 1 warning toàn repo. Trong phạm vi S2.4:
  - `RoadmapNode.jsx:9` — `'ON_SURFACE' is assigned a value but never used` (no-unused-vars)
  - `RoadmapNode.jsx:11` — `'OUTLINE_VARIANT' is assigned a value but never used` (no-unused-vars)
  - Các lỗi còn lại NGOÀI phạm vi: `TopicModal.jsx` (S2.5), `ConversationPage.jsx` (S7.1), `RoadmapSection.jsx` (pre-existing), `PronunciationPage.jsx` (S6.1).
- Đối chiếu mockup `lo_trinh_hoc_tap_snake_style/screen.png` ↔ code: layout khớp (header, snake zigzag, node màu, badge check/lock, connector cong).

## UI/Mockup Check

Đối chiếu `speak-buddi-docs/ui/lo_trinh_hoc_tap_snake_style` + `DESIGN.md`:
- ✅ Header "Lộ trình của bạn — {level_name}" với level_name màu primary `#3525cd` — khớp mockup.
- ✅ Snake zigzag: node tròn 96px, chẵn trái/lẻ phải, connector SVG Bezier (`M 0 0 C 60 0, 40 100, 100 100` đúng mockup), màu connector xanh `#a7f3d0` (completed) / xám `#e4e1ee`.
- ✅ Badge: completed → check trắng nền secondary `#006c49`; in_progress → ring cam `#f59e0b`; locked → lock nền surface-variant + opacity/grayscale. Khớp mockup.
- ✅ Font Be Vietnam Pro, palette bám DESIGN.md, React + Bootstrap-style inline (không Tailwind) đúng yêu cầu.
- ✅ Responsive: container `maxWidth:840` + `clamp` padding; tablet ≤768px thu hẹp snake-row; mobile ≤480px chuyển sang danh sách thẳng đứng (trade-off đã ghi trong plan §6, chấp nhận được).
- ✅ Touch target: node 96px ≥ 44px; nút CTA/empty/error `minHeight ≥ 36–44px`.
- 🟢 Khác biệt nhỏ chấp nhận được: icon dùng SVG inline thay Material Symbols (không có field icon trong data + plan đã cho phép); `is_interest` thể hiện bằng badge sao nhỏ (nice-to-have, không phải AC).

## Code Quality

- ✅ Tách component rõ ràng (Page / Header / Path / Node / EmptyState / Skeleton), đúng cấu trúc feature-folder.
- ✅ Chống double-fetch StrictMode bằng `cancelled`/`cancelledRef`, cleanup trong return effect — pattern nhất quán với VocabularyPage.
- ✅ 4 trạng thái runtime (loading / error+retry / empty / data) đầy đủ và đúng nhánh; error box màu `#ffdad6/#ba1a1a` + nút "Thử lại" tăng `retryCount` để re-fetch.
- ✅ Logic mapping `status → style` đầy đủ (completed/in_progress/locked/available), hoạt động đúng cả khi mọi node = "available" (placeholder S2.2).
- ✅ BE không bị sửa; `roadmapService.js` giữ nguyên từ S2.2.
- 🟡 Dead code: `RoadmapNode.jsx` khai báo `ON_SURFACE` và `OUTLINE_VARIANT` nhưng không dùng (gây 2 lint error). Implement log mục "Lệnh đã chạy" khẳng định "các file mới của S2.4 không có error nào" — không còn chính xác (file có thể đã chỉnh trong S2.5). Nên xóa 2 hằng thừa.

## Issues

### 🟡 Non-blocking (nên sửa)
- `speak-buddi/src/features/roadmap/components/RoadmapNode.jsx:9,11` — xóa 2 hằng không dùng `ON_SURFACE`, `OUTLINE_VARIANT` để lint sạch trong phạm vi story.

### 🟢 Quan sát thêm (ngoài phạm vi)
- `AppLayout.jsx:14–19` (pre-existing) — các key `PAGE_TITLES` của dashboard/speaking/vocabulary/profile/translate dùng dấu `\` thay vì `/` (vd `"\dashboard"`) nên không match `pathname` → fallback title "SpeakBuddi". Key `"/roadmap"` của S2.4 đúng (forward slash) nên hoạt động. Lỗi này không thuộc S2.4 nhưng nên fix ở story khác.
- Lint errors ở `TopicModal.jsx` (S2.5), `ConversationPage.jsx` (S7.1), `RoadmapSection.jsx` (pre-existing) — xử lý trong story tương ứng.
- File `RoadmapPage/Path/Node` đã được mở rộng bởi S2.5 (`onNodeClick`, `TopicModal`) — đúng kỳ vọng, không ảnh hưởng AC S2.4.

## Summary

Cả 4 AC (AC-04-01 → AC-04-04) đều đạt: roadmap render đúng theo level + order_index, fetch mới mỗi mount không cache, route nằm trong ProtectedRoute với 401 redirect login, và empty state giữ nguyên header level mà không gọi mutation. UI bám sát mockup `lo_trinh_hoc_tap_snake_style` và DESIGN.md, đủ 4 trạng thái runtime và responsive. Chỉ còn 2 lỗi lint dead-code nhỏ trong `RoadmapNode.jsx` (hằng không dùng) — non-blocking, không ảnh hưởng AC nên không chặn PASS; khuyến nghị dọn ở lần chạm tiếp theo.
