# Review — S9.1: Admin CRUD topic/vocabulary/test + validation inline

- **Ngày:** 2026-06-07
- **Verdict:** PASS

## Đối chiếu Acceptance Criteria

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-13-01 — lưu nội dung hợp lệ | ✅ đạt | BE: 13 endpoint trong `admin_content.py` (topics/words/tests CRUD) + `include_router` trong `main.py`. FE: `AdminTopicsPage`, `AdminVocabularyPage`, `AdminTestsPage`, `AdminTestEditorPage` gọi `adminContentService.js`; toast + refresh list sau lưu. |
| AC-13-02 — validation inline khi sai | ✅ đạt | Client: `validateTopicForm` / `validateWordForm` + `ContentFormModal` hiển thị lỗi dưới field (viền đỏ). BE: 409 trùng topic/word, 400 thiếu field; FE `mapApiErrorToFields` map vào `name`/`slug`/`word`. Editor: viền đỏ + badge `MISSING CORRECT ANSWER` + đếm Incomplete. |
| §5.2 — message field bắt buộc trống | ✅ đạt | FE `REQUIRED_MSG = "⚠ Vui lòng điền đầy đủ thông tin."`; BE trả `"Vui lòng điền đầy đủ thông tin."` → FE normalize qua `mapApiErrorToFields` khi có chuỗi `điền đầy đủ`. |
| BR07 — chỉ admin truy cập | ✅ đạt | Router `dependencies=[Depends(require_admin)]`; FE `AdminRoute` redirect guest → `/login`, student → `/`. |

## Kiểm chứng đã chạy

- **FE `npm run lint`** → 7 errors, toàn bộ trong `RoadmapSection.jsx` (5) + `RoadmapNode.jsx` (2) — pre-existing, ngoài phạm vi S9.1. **0 lỗi** trong `features/admin/**` và `App.jsx` (route admin).
- **BE `python -m py_compile`** → pass: `admin_content.py`, `content_repo.py`, `quiz_repo.py`.
- **Route** — `App.jsx`: `AdminRoute` → `AdminLayout` → `/admin/topics`, `/admin/vocabulary`, `/admin/tests`, `/admin/tests/new`, `/admin/tests/:id/edit`, redirect `/admin` → `/admin/topics`.
- **Service coverage** — `adminContentService.js` khớp 13 endpoint plan (levels, topics×4, words×4, tests×4).
- **Phạm vi loại trừ** — soft delete (`is_active=false`) không có trong UI/API S9.1 → đúng plan (thuộc S9.2).

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa
- **Smoke test runtime chưa chạy trong session review:** chưa verify POST topic/word/test trên PostgreSQL thật + curl 403 student vs 200 admin. Code review xác nhận logic đúng; `created_by` nullable nên mock admin RAM không vi phạm FK. Khuyến nghị QA chạy checklist trong `implement/S9.1-implement.md` trước production.
- **Sidebar mobile:** `AdminSidebar` ẩn hoàn toàn ≤1024px, chưa có hamburger — nội dung admin vẫn truy cập được nhưng thiếu nav. Ghi nhận trong plan §6; không chặn PASS (pattern giống S11.1 skeleton).

### 🟢 Quan sát thêm (ngoài phạm vi)
- `/admin/dashboard` (Overview) chưa có page — thuộc S11.1; sidebar vẫn trỏ route đó.
- BE `grammar_mapping` editor gửi answers nhưng validation đáp án đúng chỉ bắt buộc với `multiple_choice` — khớp BE `_validate_test_editor`.
- Không có pytest/integration test tự động cho admin API — giới hạn hạ tầng test repo.

## Kết luận

Story S9.1 **PASS**: CRUD admin topic/vocabulary/test end-to-end (BE + FE), validation inline AC-13-02/§5.2, phân quyền BR07 đều được implement đúng plan. Không có blocker code. Nợ nhỏ: smoke test thủ công trên DB thật và nav mobile — không cần quay lại implement trừ khi QA phát hiện regression runtime.
