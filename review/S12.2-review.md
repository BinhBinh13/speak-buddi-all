# Review — S12.2: Người dùng yêu cầu xóa dữ liệu cá nhân

- **Ngày:** 2026-06-07
- **Verdict:** PASS

## Đối chiếu Acceptance Criteria

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-S12.2-1 (§4.7) — Luồng xóa in-app | ✅ đạt | `DeleteAccountSection` trên `/profile`; `DELETE /api/profile/account` với `Depends(current_user)`. |
| AC-S12.2-2 (§5.2) — Xác nhận trước xóa | ✅ đạt | Modal bắt gõ `XOA`; password bắt buộc khi `password_hash` có (BE + FE `has_password`). |
| AC-S12.2-3 (§4.6) — Purge PII | ✅ đạt | `delete_user_personal_data()` xóa translation/progress/quiz/oauth/session/reset-token/voice pref; clear `user_profile`; cancel active subscription. |
| AC-S12.2-4 (§4.7) — Vô hiệu hóa tài khoản | ✅ đạt | `users.status='deleted'`, email `deleted+<uuid>@speakbuddi.invalid`, `password_hash=NULL`; `get_user_by_email/id` lọc `status != 'deleted'`. |
| AC-S12.2-5 — Logout sau xóa | ✅ đạt | `logout()` + `sessionStorage.auth_msg` + `navigate('/')`. |
| AC-S12.2-6 (§4.8/§4.9) — UI tiếng Việt | ✅ đạt | Danger Zone bám mockup `ho_so_cai_dat_desktop`; nút minHeight 44px; lỗi `role="alert"`. |
| AC-S12.2-7 (BR07) — Chặn admin | ✅ đạt | `profile.py:96-100` → 403 nếu `role=admin`. |
| AC-S12.2-8 (§4.5) — Log an toàn | ✅ đạt | Chỉ `ACCOUNT_DELETE ok user=<id>`; không log password/confirm_text. |
| AC-S12.2-9 — Cập nhật Privacy Policy | ✅ đạt | `PrivacyPolicyPage.jsx` mục 6 trỏ `/profile` + giữ email dự phòng. |

## Kiểm chứng đã chạy

- **BE `python -m py_compile`** — pass: `routers/profile.py`, `repositories/user_repo.py`, `schemas/profile.py`, `schemas/auth.py`.
- **FE `npx eslint`** (ProfilePage, DeleteAccountSection, ConfirmDeleteAccountModal, profileService, PrivacyPolicyPage) → **0 lỗi**.
- **Đối chiếu code vs plan/implement log** — khớp endpoint, repo transaction 10 bước, FE Danger Zone + modal, `has_password` trong `user_to_dict`.

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa
- **Smoke PostgreSQL E2E** chưa chạy trong session review — nên verify DELETE trên DB thật (đặc biệt nếu chưa migrate `translation_history` / `user_voice_preference`).
- **Session FE cũ** có thể thiếu `has_password` → FE mặc định yêu cầu password (an toàn); user OAuth-only re-login/`/me` sẽ thấy field đúng.
- **Epic 6/7** chưa có bảng audio/transcript — cần mở rộng `delete_user_personal_data()` khi schema có (đã ghi trong plan).

### 🟢 Quan sát thêm (ngoài phạm vi)
- Giữ `payment_transaction` gắn user anonymized — đúng quyết định plan phục vụ S11 analytics.
- Email gốc được giải phóng sau anonymize → user có thể đăng ký lại cùng email (chấp nhận được v1.1.0).

## Kết luận

Story S12.2 **PASS**: luồng self-service xóa dữ liệu cá nhân end-to-end (FE Danger Zone + BE purge/anonymize) đáp ứng §4.6/§4.7, có xác nhận §5.2, chặn admin, cập nhật Privacy Policy. Không blocker. Nợ nhỏ: smoke test PostgreSQL và mở rộng DELETE khi Epic 6/7 có schema.
