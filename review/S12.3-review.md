# Review — S12.3: Form liên hệ / hỗ trợ + support email

- **Ngày:** 2026-06-07
- **Verdict:** PASS

## Đối chiếu Acceptance Criteria

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-S12.3-1 (§28) — Form công khai | ✅ đạt | Route `/contact`; `ContactPage` có họ tên, email, select chủ đề, textarea nội dung. |
| AC-S12.3-2 (§28) — Email support | ✅ đạt | `send_support_contact_email()` — `To`/`From` = `SMTP_USER`, `Reply-To` = email user; không env `SUPPORT_EMAIL` riêng (đúng quyết định PO). |
| AC-S12.3-3 (§5.2) — Validation BE | ✅ đạt | `routers/support.py` + Pydantic max_length; message tiếng Việt 400. |
| AC-S12.3-4 (§4.8/§4.9) — UI | ✅ đạt | Tiếng Việt, design tokens, nút `minHeight: 44`, lỗi `role="alert"`. |
| AC-S12.3-5 (§4.5) — Log an toàn | ✅ đạt | Chỉ `SUPPORT_CONTACT ok/skip/honeypot`; email service không log nội dung/message. |
| AC-S12.3-6 — Footer links | ✅ đạt | `PublicFooter` — "Liên hệ"/"Trợ giúp" → `/contact`. |
| AC-S12.3-7 — Entry points phụ | ✅ đạt | `PrivacyPolicyPage` mục 8 + `PaymentResultPage` link hỗ trợ. |
| AC-S12.3-8 — Pre-fill auth | ✅ đạt | `useState(() => user?.name/email)` từ `useAuth()`. |
| AC-S12.3-9 — Success state | ✅ đạt | Form ẩn, hộp xác nhận + link về trang chủ. |

## Kiểm chứng đã chạy

- **BE `python -m py_compile`** — pass: `schemas/support.py`, `routers/support.py`, `services/email_service.py`, `main.py`.
- **FE `npx eslint`** (ContactPage, supportService, PublicFooter, PaymentResultPage, PrivacyPolicyPage, App) → **0 lỗi**.
- **Đối chiếu code vs plan/implement log** — khớp endpoint, honeypot, optional JWT `user_id`, HTML escape trong email body.

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa
- **Smoke E2E gửi email** chưa chạy trong session review — nên submit form 1 lần trên BE + SMTP thật để xác nhận inbox `SMTP_USER` nhận mail và Reply-To hoạt động.
- **Privacy Policy** vẫn hiển thị `mailto:support@speakbuddi.vn` placeholder bên cạnh form `/contact` — chấp nhận được v1; có thể đồng bộ copy sau nếu PO muốn bỏ placeholder.
- **Không rate limit / captcha** — đã ghi trong plan; theo dõi nếu bị spam.

### 🟢 Quan sát thêm (ngoài phạm vi)
- Dev env không cấu hình SMTP → API vẫn 200 + `SUPPORT_CONTACT skip` (pattern nhất quán với reset email).
- `optional_current_user` gắn `user_id` vào email khi đã login — hữu ích cho support.

## Kết luận

Story S12.3 **PASS**: form liên hệ công khai `/contact` end-to-end (FE + BE), email chuyển tiếp tới inbox `SMTP_USER`, footer và entry points phụ đã cập nhật, validation/log/UI đúng plan. Không blocker. Nợ nhỏ: smoke test SMTP E2E một lần trước production.
