# Review — S3.4: Hiển thị nội dung crawl mới, giữ tiến độ cũ

- **Ngày:** 2026-06-06
- **Verdict:** PASS

## Đối chiếu Acceptance Criteria

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-05-04 (a) — từ mới crawl hiển thị | ✅ đạt | `GET /api/topics/{id}/words` filter `WHERE tw.is_active = TRUE` (learning.py:118); từ Langeek active tự xuất hiện. FE `handleSelectTopic` gọi lại `getWords` mỗi lần mở topic (VocabularyPage.jsx:162), không cache stale. Verify script kịch bản A insert `greet` source='langeek' active. |
| AC-05-04 (b) — từ cũ giữ progress | ✅ đạt | `handleSelectTopic` reset rồi hydrate `progressMap` keyed theo `topic_word_id` (VocabularyPage.jsx:156,184); endpoint progress JOIN active nên chỉ trả progress từ active, không reset row cũ. Kịch bản B SELECT xác nhận hello/goodbye giữ status. |
| AC-05-04 (c) — soft-disable giữ lịch sử DB | ✅ đạt | FK `topic_word_id ... ON DELETE CASCADE` (schema_progress.sql:31) chỉ kích khi hard-delete. Soft-disable = `UPDATE is_active=FALSE` → row progress còn nguyên. Kịch bản C có 2 SELECT: C.1 (words API ẩn hello), C.2 (progress row hello vẫn còn). Khớp BR11. |
| AC-05-04 (d) — percent_known re-base | ✅ đạt | `total_words` = COUNT active hiện tại (learning.py:248-256); per-word progress JOIN `tw.is_active=TRUE` (learning.py:266-267) nên từ disabled (hello=known) bị loại khỏi known_count. Kịch bản D query mô phỏng đúng công thức endpoint: total=5, known=0, percent=0%. |
| (UX) Badge "Mới" | ✅ đạt | `NewWordBadge` render khi `word.source === 'langeek'` AND `progressStatus == null` (Flashcard.jsx:41), ẩn khi đã có progress. Optimistic update qua `setProgressMap` làm badge biến mất ngay sau khi đánh dấu. |

## Kiểm chứng đã chạy

- `cd speak-buddi && npm run lint` → **5 lỗi, đều trong `RoadmapSection.jsx`** (no-unused-vars + rules-of-hooks). Pre-existing, ngoài phạm vi S3.4. **0 lỗi** trong `Flashcard.jsx` và `VocabularyPage.jsx` (file của story này).
- `cd speak-buddi-be && python -m py_compile routers/learning.py` → **PY_COMPILE_OK**.
- Đối chiếu code BE thật (learning.py) với khẳng định trong plan/log: tất cả filter `is_active=TRUE` và JOIN active đúng như mô tả — không phải log suông.
- Đối chiếu schema thật: FK CASCADE + comment chuẩn bị AC-05-04 (schema_progress.sql:26-27,31) đúng.
- Đối chiếu seed: level A1, topic `greetings-introductions`, 5 từ (hello/goodbye/name/meet/introduce) tồn tại trong `schema_learning.sql:154-265` (KHÔNG ở `seed_dev.sql` — xem mục Nên sửa). Verify script header (dòng 2-6) liệt kê đúng schema_learning trước seed_dev nên prerequisite vẫn thỏa.
- Demo user UUID `00000000-0000-0000-0000-000000000002` khớp `seed_dev.sql:27`. Verify script dùng đúng UUID này.

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa
- **[implement/S3.4-implement.md:34]** Hướng dẫn test thủ công ghi từ vựng/topic được seed bởi `seed_dev.sql`, nhưng thực tế chúng nằm trong `schema_learning.sql`. `seed_dev.sql` chỉ seed users/profile. Câu lệnh ở dòng 34 (chạy schema_core + schema_learning + schema_progress + seed_dev) vẫn cho kết quả đúng vì schema_learning chứa seed; chỉ là chú thích "đã insert bởi verify script / seed" hơi sai địa chỉ — gây nhầm khi debug. Không ảnh hưởng AC.
- **[verify_s3_4.sql]** Script idempotent tốt (DO block + IF NOT EXISTS + ON CONFLICT) nhưng để lại side-effect (greet được thêm, hello bị disable) sau khi chạy; khối CLEANUP bị comment. Nếu reviewer/dev chạy rồi vào FE thật sẽ thấy state đã đổi. Nên ghi rõ "bỏ comment CLEANUP để reset" — hiện đã có note ở dòng 207-208, chấp nhận được.

### 🟢 Quan sát thêm (ngoài phạm vi)
- Màu emerald `#006c49`/`#6cf8bb` theo DESIGN.md được mô tả "reserved strictly for positive reinforcement / completed / achievement". Badge "Mới" mang nghĩa thông tin (nội dung mới) hơn là thành tựu — dùng emerald là hợp palette nhưng hơi lệch ngữ nghĩa. Không chặn vì vẫn trong design system và đọc tự nhiên.
- 5 lỗi lint `RoadmapSection.jsx` cần dọn trong story riêng (developer đã note ở dòng 55 implement log).
- Ràng buộc "crawler S9.3/S9.5 chỉ soft-delete, cấm hard-delete topic_word" cần được dev Epic 9 tuân thủ — đã ghi trong implement log + schema comment. Đề nghị nhắc lại khi plan S9.3.

## Kết luận

Toàn bộ AC-05-04 (a–d) đạt: lõi preservation đã được thiết kế S3.1/S3.2/S3.3 bảo đảm (filter `is_active`, JOIN active, CASCADE chỉ-hard-delete) và được kiểm chứng trực tiếp trên code BE thật, không tin log suông. Badge "Mới" trên `Flashcard.jsx` render/ẩn đúng điều kiện và đúng style DESIGN.md. `verify_s3_4.sql` có đủ 4 kịch bản (A–D) idempotent, dùng đúng UUID demo, comment kỳ vọng rõ. Lint sạch trong phạm vi story (5 lỗi còn lại pre-existing ngoài scope), py_compile pass. Hai mục "Nên sửa" chỉ là chú thích tài liệu, không ảnh hưởng correctness → PASS.
