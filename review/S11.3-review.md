# Review — S11.3: Export Excel/PDF + lưu lịch sử

- **Ngày:** 2026-06-07
- **Verdict:** PASS

## Đối chiếu Acceptance Criteria

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-12-03 — Admin export → Excel/PDF + ghi history | ✅ đạt | `POST /export` stream file; `create_export` → `mark_completed`; `GET /exports` + `ExportHistoryTable`. |
| §3.6 — 4 nhóm metric analytics | ✅ đạt | `revenue`, `users`, `learning`, `ai_usage` (AI placeholder khi `is_available=false`). |
| §4.1 — export đồng bộ < 30s | ✅ đạt | Sinh sync, cap 1000 tx / 500 attempts; không job queue. |
| Admin-only | ✅ đạt | `require_admin` trên `/export`, `/exports`; FE `/admin/reports` trong `AdminRoute`. |
| Validation §5.2 | ✅ đạt | `from > to` → 400; `plan_id` ngoài revenue → 400; plan không tồn tại → 404; FE validate ngày inline. |
| Zero state | ✅ đạt | `export_builders` ghi `EMPTY_MSG` khi không có detail rows; history vẫn `completed`. |
| §4.5 logging | ✅ đạt | Log `admin_id`, `report_type`, `export_format`, `export_id`, `status` — không log email/amount từng dòng. |

## Kiểm chứng đã chạy

- **FE `npx eslint`** (reports + `reportExportService.js`) → **0 lỗi**.
- **BE `python -m py_compile`** → pass: `admin_analytics.py`, `report_export_repo.py`, `export_builders.py`, `report_export_service.py`, `analytics.py`.
- **Routing** — `App.jsx` `/admin/reports`; sidebar Reports active; `AdminLayout` title map.
- **Migration** — `007_report_export_history.sql` tồn tại, khớp schema plan.

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa
- **Smoke PostgreSQL E2E** chưa chạy — cần migrate `007_*` + `pip install openpyxl reportlab` trước demo.
- **`exportReport`** — fetch blob chưa refresh token 401 (cùng pattern S11.1/S11.2).
- **Re-download file cũ** — MVP chỉ audit log, không lưu file server (đúng plan).

### 🟢 Quan sát thêm (ngoài phạm vi)
- Mockup có CSV/JSON — implement đúng SRS (chỉ xlsx/pdf).
- PDF chi tiết cap 50 dòng — Excel đủ cap; ghi chú trong PDF.

## Kết luận

Story S11.3 **PASS**: export Excel/PDF end-to-end (AC-12-03), lịch sử trong `report_export_history`, trang `/admin/reports` khớp mockup Export Engine. EPIC-11 hoàn thành sau review này. Không blocker — nợ nhỏ: smoke DB và export 401 refresh (tùy chọn).
