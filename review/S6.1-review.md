# Review — S6.1: Ghi âm qua mic + xử lý lỗi mic (denied/not found/unsupported)

- **Ngày:** 2026-06-07
- **Verdict:** PASS

## Đối chiếu Acceptance Criteria

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-08-01 (cấp quyền → thu audio) | ✅ đạt | `useMediaRecorder.start()` gọi `getUserMedia({audio:true})`, tạo `MediaRecorder`, gom chunks; `onstop` ghép `Blob`, nếu `size ≥ 1000` → state `done` + lưu `audioBlob`; `PronunciationPage` render `ScorePlaceholder` ("Đang chấm điểm..."). |
| AC-08-02 (denied/notfound → message) | ✅ đạt | `mapGetUserMediaError()` map DOMException → `{code,message}`, render qua `MicErrorAlert`. |
| §5.2 — denied | ✅ đạt | `micErrors.js:22` khớp chính xác chuỗi SRS dòng 659. Map từ `NotAllowedError/SecurityError/PermissionDeniedError`. |
| §5.2 — notfound | ✅ đạt | `micErrors.js:29` khớp SRS dòng 661. Map từ `NotFoundError/DevicesNotFoundError/OverconstrainedError`. |
| §5.2 — unsupported | ✅ đạt | `micErrors.js:36` khớp SRS dòng 662. Check `!navigator.mediaDevices?.getUserMedia \|\| typeof MediaRecorder === "undefined"` TRƯỚC getUserMedia (`useMediaRecorder.js:50`). |
| §5.2 — noaudio | ✅ đạt | `NOAUDIO_ERROR` (`micErrors.js:43`) khớp SRS dòng 660. Kích hoạt khi `blob.size < 1000` (`useMediaRecorder.js:107`). |
| Route `/pronunciation` trong ProtectedRoute | ✅ đạt | `App.jsx:24,66` — import + `<Route path="/pronunciation">` đặt trong khối `<ProtectedRoute />`. |
| Mic cleanup (stop/reset/unmount) | ✅ đạt | `getTracks().forEach(t=>t.stop())` ở: `recorder.onstop` (L105), `reset()` (L148), unmount effect (L43), và cả khi start dọn phiên cũ (L62) + unmount giữa lúc chờ permission (L82). |
| State machine idle→recording→processing→done/error | ✅ đạt | Đúng thứ tự; `processing` set khi stop/auto-stop, `done`/`error` set trong `onstop`. |
| maxDurationMs auto-stop | ✅ đạt | `setTimeout` 15000ms tự gọi `recorder.stop()` + set `processing` (L126-131). |
| `audioBlob` expose từ hook (cho S6.2) | ✅ đạt | Trả về trong object hook (L157). Page chưa consume — đúng phạm vi S6.1. |
| `useLocation().state` + fallback | ✅ đạt | `PronunciationPage.jsx:27-31` đọc state, fallback `FALLBACK_WORD` ("Departure"). |
| UI bám mockup + touch ≥44px + responsive | ✅ đạt | Inline style theo DESIGN.md (primary `#3525cd`, `#006c49` n/a, font Be Vietnam Pro), nút mic 80px, mọi nút có `minHeight:44`, layout `maxWidth` center → responsive. |
| Không sửa backend | ✅ đạt | Không có thay đổi trong `speak-buddi-be/` cho story này. |
| Không sửa `features/speaking/` | ✅ đạt | `pronunciation/` là folder mới độc lập. |
| Không log audio (§4.5) | ✅ đạt | Không có `console.*` nào trong feature (grep xác nhận). |

## Kiểm chứng đã chạy

- `npm run lint` (trong `speak-buddi/`) → **8 errors, 0 warnings**. Tất cả pre-existing và NGOÀI phạm vi S6.1:
  - `RoadmapSection.jsx` (5: unused vars + 1 hook gọi có điều kiện)
  - `RoadmapNode.jsx` (2 unused vars)
  - `TopicModal.jsx` (1 unused var)
  - **0 lỗi/0 warning trong toàn bộ file của S6.1** (pronunciation/* + App.jsx). Khớp với khai báo trong implement log.
- Đối chiếu 4 chuỗi message với SRS §5.2 (dòng 659-662) → khớp ký tự (kể cả emoji 🎤 và mũi tên →).
- Grep `console.|blob|audioBlob` trong feature → không có log audio.

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa
- `useMediaRecorder.js`: code lặp lại logic cleanup timer/autoStop/stream ở 4 chỗ (start, onstop, stop, reset, unmount). Có thể gom vào 1 helper `cleanupSession()` cho dễ bảo trì. Không ảnh hưởng correctness.
- `RecordButton.jsx` và `PronunciationPage.jsx` cùng inject `<style>` keyframes (`sb-pulse`, `sb-spin`) inline mỗi render. Hoạt động đúng nhưng nên cân nhắc đưa vào CSS toàn cục. Không chặn.
- `PronunciationPage.jsx` có 1 nút "Thử lại" riêng (L88) ngoài nút bên trong `MicErrorAlert` (L16) — khi `status === "error"` sẽ hiện 2 nút "Thử lại" cạnh nhau (banner + nút page). Trùng lặp nhẹ về UX, nên bỏ 1 trong 2. Không chặn AC.

### 🟢 Quan sát thêm (ngoài phạm vi)
- 8 lint error pre-existing ở Epic roadmap (`RoadmapSection.jsx` có hook gọi có điều kiện — lỗi rules-of-hooks tiềm ẩn runtime) nên được xử lý ở story liên quan.
- `OverconstrainedError` được map về NOTFOUND theo plan; thực tế nó thường do constraint chứ không hẳn thiếu thiết bị, nhưng đây là quyết định đã chốt trong plan §3 nên chấp nhận.

## Kết luận

PASS. Mọi AC trong phạm vi S6.1 (AC-08-01, AC-08-02 + 4 message §5.2) đều đạt; state machine, auto-stop, cleanup mic, check unsupported-trước-getUserMedia, expose `audioBlob` cho S6.2 đều implement đúng và khớp plan. Lint sạch hoàn toàn ở các file của story (8 lỗi còn lại là pre-existing ngoài phạm vi). Không có blocker, không sửa backend/speaking, không log audio. Ba điểm "nên sửa" (2 nút Thử lại trùng, code lặp cleanup, style inline) là chất lượng nhẹ, không ảnh hưởng nghiệm thu → chuyển story sang `done`.
