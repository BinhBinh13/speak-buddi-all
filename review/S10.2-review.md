# Review — S10.2: Soft delete payment plan

- **Ngày:** 2026-06-07
- **Verdict:** PASS

## Đối chiếu Acceptance Criteria

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-14-03 — disable + confirm → is_active=false | ✅ đạt | FE: `ConfirmDisableModal` + `disablePlan()`; BE: `DELETE /api/admin/payment-plans/{id}` → `soft_delete_plan` UPDATE `is_active=FALSE` + commit + log `ADMIN_DISABLE_PAYMENT_PLAN`. |
| §5.3 — soft delete, không hard delete | ✅ đạt | Chỉ `UPDATE is_active`; không `DELETE` row; không đổi schema. |
| Guest/học viên không thấy gói inactive | ✅ đạt | `payment_repo.list_active_plans` + `get_active_plan` filter `is_active=TRUE` (không regress). |
| Admin thấy inactive + filter | ✅ đạt | Badge "Đã tắt" (S10.1); filter Tất cả/Đang bán/Đã tắt; nút Vô hiệu hóa ẩn khi đã inactive. |
| BR07 — chỉ admin | ✅ đạt | Router `dependencies=[Depends(require_admin)]`; route FE dưới `AdminRoute`. |

## Kiểm chứng đã chạy

- **FE `npx eslint src/features/admin/payment-plans`** → 0 lỗi.
- **BE `python -m py_compile`** → pass: `payment_plan_admin_repo.py`, `admin_payment_plan.py`.
- **Router** — DELETE endpoint 204 khi OK, 404 khi id không tồn tại; idempotent (disable lại gói đã tắt vẫn 204).
- **Read-side** — xác nhận code `payment_repo.py` lines 42, 62: `is_active = TRUE`.

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa
- **`disablePlan` / `apiDelete` không refresh token 401** như `apiClient` — edge case hiếm, không chặn AC (cùng pattern S9.2).
- **Modal confirm** dùng `entityLabel` tùy chỉnh (Pricing/mua mới) — body vẫn kết thúc câu hỏi chung của `ConfirmDisableModal`; chấp nhận cho MVP.

### 🟢 Quan sát thêm (ngoài phạm vi)
- Re-enable gói — ngoài AC-14-03; `PUT is_active=true` có thể story sau.
- Smoke PostgreSQL E2E chưa chạy trong session review.
- Disable gói có subscription active không revoke — đúng BR04/plan.

## Kết luận

Story S10.2 **PASS**: soft delete payment plan end-to-end (AC-14-03) với confirm modal, filter admin, read-side public đã lọc inactive. Không blocker. Nợ nhỏ: smoke DB thủ công và apiDelete 401 refresh (tùy chọn).
