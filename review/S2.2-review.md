# Review — S2.2: Sinh roadmap cá nhân hóa từ level + mục tiêu học

- **Ngày:** 2026-06-06
- **Verdict:** PASS

## Đối chiếu Acceptance Criteria

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-03-02 | ✅ đạt | `GET /api/roadmap` đọc `target_level` đã lưu từ onboarding (S2.1) và sinh roadmap từ active content của level đó. Interests xếp trước (`ORDER BY ... DESC`), gắn `is_interest=true`. |
| AC-04-01 | ✅ đạt | Query `JOIN level l ON t.level_id = l.id WHERE l.code = UPPER(:level_code)` → mọi node thuộc đúng level user. Topic level khác không xuất hiện. |
| AC-04-02 / BR11 | ✅ đạt | `WHERE t.is_active = TRUE` trong `get_roadmap_topics`; `word_count` cũng chỉ đếm `topic_word.is_active = TRUE`. Content disabled biến mất khỏi roadmap. |
| AC-04-04 | ✅ đạt | Chưa onboard (`target_level` NULL) → trả 200, `nodes=[]`, `total_topics=0`, không raise lỗi, không động vào `user_profile`. Không có topic active → list rỗng tự nhiên. Không 500. |
| AC-04-03 (Guest) | ✅ đạt | `Depends(current_user)` raise 401 khi thiếu/invalid JWT (xác nhận trong `auth/deps.py`). |
| BR09 | ✅ đạt | `VALID_LEVELS = {A1..C2}`; router validate `level.upper()` nếu đã set, raise 400 nếu lệ. Query dùng `UPPER(:level_code)` để chuẩn hóa. |
| Data contract cho S2.4 | ✅ đạt (theo plan) | `RoadmapNode`: `id, name, slug, description, order_index, is_interest, status, word_count`. `RoadmapOut`: `level, level_name, total_topics, selected_topics, nodes`. Khớp đúng contract plan §3. Xem ghi chú ở Phát hiện về khác biệt tên field so với checklist task. |
| Không log dữ liệu cá nhân | ✅ đạt | Log chỉ ghi `user=<id> level=<code> total=<n>`; KHÔNG log danh sách interests. Đúng SRS §4.5. |
| `npm run lint` không lỗi mới | ✅ đạt | 5 lỗi tồn tại trong `dashboard/components/RoadmapSection.jsx` — file KHÔNG thuộc S2.2 (không nằm trong diff/file mới). File mới `roadmapService.js` lint sạch. |

## Kiểm chứng đã chạy

- `cd speak-buddi && npm run lint` → 5 errors, tất cả ở `src/features/dashboard/components/RoadmapSection.jsx` (CR/isCurrent/totalXP/progress unused + useNavigate conditional). Đây là lỗi PRE-EXISTING — `git status` xác nhận file này không nằm trong thay đổi của S2.2; file của story (`roadmap/services/roadmapService.js`) không có lỗi.
- `git status` các thư mục liên quan → xác nhận file mới: `roadmap_repo.py`, `routers/roadmap.py`, `schemas/roadmap.py`, `features/roadmap/`; `main.py` đã include `roadmap.router` (line 7 import, line 26 include_router).
- Đối chiếu query với schema thật (`db/schema_learning.sql`, `db/schema_core.sql`):
  - `level.code`, `level.name`, `topic.level_id`, `topic.slug`, `topic.description`, `topic.display_order`, `topic.is_active` — tồn tại đúng.
  - Subquery `COUNT(*) FROM topic_word WHERE topic_id = t.id AND is_active = TRUE` — cột đúng (`topic_word.topic_id`, `topic_word.is_active`), có index `idx_topic_word_topic_active`.
  - `user_profile.target_level VARCHAR(10)`, `user_profile.interests TEXT[]` — khớp; `COALESCE(p.interests, ARRAY[]::TEXT[])` an toàn với NULL.

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa
- `routers/roadmap.py:59` — f-string không có placeholder (`f"Level không hợp lệ..."`). Vô hại nhưng thừa prefix `f`. Cosmetic.

### 🟢 Quan sát thêm (ngoài phạm vi)
- **Tên field data contract khác checklist task:** checklist trong yêu cầu review nêu `topic_id`, `level_code`, `level_name`, `is_selected` ở node; code dùng `id`, `is_interest`, và đặt `level`/`level_name` ở cấp `RoadmapOut` (không lặp mỗi node). Code khớp **plan §3** (nguồn sự thật của contract) nên không tính là sai. S2.4 chỉ cần đọc theo schema thật ở `roadmapService.js` JSDoc — đã được ghi rõ. Khi vào S2.4 cần thống nhất là OK.
- **Interests lưu nguyên giá trị FE gửi:** `update_onboarding` lưu `req.topics` trực tiếp; roadmap query so cả `t.slug = ANY(:interests) OR t.name = ANY(:interests)` nên match dù FE gửi slug hay name — phòng vệ tốt. Không cần sửa.
- **`status` luôn `"available"`:** placeholder cho tracking tiến độ per-topic (chưa có bảng `user_topic_progress`). Đúng như plan §6 TBD; snake-style mockup hiển thị completed/active/locked sẽ cần bơm dữ liệu thật ở story tracking sau.
- **Seed chỉ có topic A1/A2:** level B1–C2 sẽ trả `nodes=[]` (đúng AC-04-04 nhưng lưu ý khi demo).
- **5 lỗi ESLint trong `RoadmapSection.jsx`** (đặc biệt `useNavigate` gọi có điều kiện — vi phạm rules-of-hooks, có thể gây bug runtime) — ngoài phạm vi S2.2, nên xử lý ở task/story riêng.

## Kết luận

PASS. Toàn bộ AC chính (AC-03-02) và các AC hỗ trợ (AC-04-01/02/04, BR09/BR11, Guest 401) đều đạt; query đối chiếu đúng schema DB thật, empty state và chưa-onboard xử lý an toàn không 500, không log dữ liệu cá nhân. Data contract khớp plan §3 và đủ ổn định để S2.4 consume. Lint không phát sinh lỗi mới (5 lỗi tồn tại nằm ngoài phạm vi story). Story chuyển sang `done`. Khuyến nghị (không chặn): dọn lỗi `RoadmapSection.jsx` ở task riêng trước khi S2.4 dùng chung khu vực dashboard roadmap.
