# Review — S1.2: Trang landing + pricing công khai (Guest xem không cần login)

- **Ngày:** 2026-06-06
- **Verdict:** PASS

## Đối chiếu Acceptance Criteria

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-01-01 | ✅ đạt | Route `/` → `<LandingPage />` trong `App.jsx:12`, **không bọc auth guard**. `LandingPage.jsx` render đầy đủ: Hero (tiêu đề VI + 2 CTA), Stats (3 số liệu), Features (6 card), PricingSection teaser. Không có lệnh gọi API backend nào trong các component → Guest xem không cần login. |
| AC-01-02 | ✅ đạt | Route `/pricing` → `<PricingPage />` trong `App.jsx:13`, **không bọc auth guard**. `PricingPage.jsx` hiển thị header "Bảng giá", 2 gói public (Free 0đ + Pro 149.000đ với badge "Phổ biến"), FAQ 3 mục. Pricing teaser cũng có sẵn trên landing (`PricingSection.jsx`). Không gọi API → Guest xem không cần login. |
| AC-01-03 | ➖ ngoài phạm vi | Chặn Guest vào tính năng học thuộc S1.3 — plan đã loại trừ rõ; chỉ đảm bảo CTA điều hướng đúng. |
| NFR §4.8 (responsive/touch) | ✅ đạt | Grid responsive qua media query: features 1→2(`768`)→3(`1024`) cột; pricing 1→2 cột; stats 3→1 cột; hero CTA full-width mobile. `minHeight: 44` trên mọi CTA/link button (Navbar CTA, Hero, PlanCard, PricingSection). |

## Kiểm chứng đã chạy

- `cd speak-buddi && npm run lint` → **11 problems (10 errors, 1 warning)**, TẤT CẢ là pre-existing ở các file ngoài phạm vi S1.2:
  - `LoginPage.jsx` — FONTS/GOOGLE_FONTS_URL unused (2)
  - `OAuthCallbackPage.jsx` — useEffect missing dependency (1 warning)
  - `RoadmapSection.jsx` — CR/isCurrent/totalXP/progress unused + conditional useNavigate (5)
  - `SpeakingPage.jsx` — setState in effect + typeReply before declaration (2)
  - `speechService.js` — manuallyStopped unused (1)
  - **Số lỗi MỚI từ S1.2: 0.** Tất cả file S1.2 (App.jsx, designTokens.js, PublicNavbar.jsx, PublicFooter.jsx, LandingPage.jsx + 4 section, PricingPage.jsx, index.html) sạch lint.

## Kiểm tra file thực tế

- **Route public:** `App.jsx:12-13` — `/` và `/pricing` đều khai báo trần, không có wrapper/guard. ✅
- **Design tokens:** `designTokens.js:7` export `UI.primary = "#3525cd"`; `UI.font = "'Be Vietnam Pro', system-ui, sans-serif"` (dòng 57); đầy đủ secondary `#006c49`, gold gradient, typography scale theo plan §7. Token tách riêng, không sửa `theme.js` cũ. ✅
- **Font Be Vietnam Pro:** `index.html:10` có `<link>` Google Fonts (400;500;600;700); `lang="vi"`, `<title>SpeakBuddi</title>`. ✅
- **Navigation fix:** `PublicNavbar.jsx:80` "Tính năng" dùng `<Link to="/#features">` (đúng yêu cầu, không phải anchor `href`); dòng 92 "Bảng giá" → `<Link to="/pricing">`; dòng 104 "Đăng nhập" → `/login`; CTA dòng 123 → `/login` (fallback /register theo plan). ✅
- **Anchor target:** `FeaturesSection.jsx:52` có `id="features"` — khớp với `/#features`. ✅
- **Hardcode màu/font:** Các component dùng `UI.*` token nhất quán; không hardcode font ngoài designTokens. Một vài hex inline cho shadow/blob decorative (vd `rgba(53,37,205,0.2)` = primary, `0 1px 4px rgba(0,0,0,0.04)`) — chấp nhận được, đồng bộ với token gốc. ✅

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa
- `PublicNavbar.jsx:24` — `pathname` đọc trực tiếp `window.location.pathname` thay vì `useLocation()` của react-router. Trên navbar fixed, khi điều hướng SPA giữa `/` và `/pricing`, `isAppRoute`/highlight không tự re-evaluate cho tới khi component re-render vì lý do khác. Phạm vi landing/pricing đều là public route nên hiện không gây lỗi nhìn thấy, nhưng nên chuyển sang `useLocation()` để đúng idiom router 7. Không chặn PASS.
- Chênh giá Pro giữa 2 mockup: teaser landing `99k/tháng` (`PricingSection.jsx:19`) vs pricing page `149.000đ/tháng` (`PricingPage.jsx:27`). Developer đã ghi TODO chờ S10.1 — đúng như plan §9 (TBD giá). Cần user/Admin chốt con số thật sau.

### 🟢 Quan sát thêm (ngoài phạm vi)
- Hero dùng gradient placeholder + emoji 📱 thay ảnh thật (mockup dùng ảnh CDN ngoài) — đã ghi TODO asset; chấp nhận cho story này.
- `AppNav`/`AppNavItem` trong PublicNavbar vẫn dùng `<a href>` (full reload) cho route app — ngoài phạm vi S1.2 (thuộc layout app sau), không ảnh hưởng landing/pricing.
- Footer link (Điều khoản/Bảo mật/Liên hệ/Trợ giúp) trỏ `href="#"` placeholder — đúng phạm vi (các trang đó chưa thuộc story này).

## Kết luận

Cả hai AC (AC-01-01, AC-01-02) đạt đầy đủ: route `/` và `/pricing` đều public, không bọc auth guard, render đủ nội dung giới thiệu + thông tin gói Free/Pro, không gọi API chặn Guest. Lint 0 lỗi mới (11 lỗi còn lại đều pre-existing ngoài phạm vi). Code bám đúng design tokens/mockup, điều hướng SPA bằng `<Link>`, responsive + touch target ≥ 44px. Các vấn đề còn lại (useLocation, giá Pro TBD, ảnh placeholder) đều minor/đã có TODO và không chặn. → **PASS**, story chuyển `done`.
