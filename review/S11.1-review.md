# Review — S11.1: Dashboard doanh thu/user/learning/AI

- **Ngày:** 2026-06-07
- **Verdict:** PASS

## Đối chiếu Acceptance Criteria

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-12-01 — Admin mở analytics, hiển thị revenue/user/learning/AI | ✅ đạt | FE `/admin/dashboard`: 4 KPI + pills + chart + TopWords + AiUsageCard. BE `GET /api/admin/analytics/overview` trả đủ 4 nhóm key. |
| §3.6 overview — tổng user, free/paid, new users, revenue, quiz, accuracy, top words, AI duration | ✅ đạt | UI hiển thị đủ chỉ số; AI duration = placeholder "Sắp có" khi `is_available=false` — đúng plan §6 (Epic 7 chưa có bảng). |
| Chỉ Admin truy cập | ✅ đạt | FE `AdminRoute` (401→login, non-admin→`/`); BE `Depends(require_admin)` trên cả `/overview` và `/timeseries` (403 student). |
| Zero/empty state an toàn | ✅ đạt | API trả 0 + flag; UI "Chưa có dữ liệu" / "Sắp có"; StatCard không trend giả. |
| NFR §4.1 — API overview P95 < 1s | ⚠️ chưa đo runtime | Repo tách 5 query overview + 1 timeseries, không N+1; `py_compile` pass — cần smoke trên PostgreSQL thật. |
| NFR §4.8 responsive | ✅ đạt (code) | KPI grid 1→2→4 cột; chart full-width mobile; touch target ≥44px trên select/nav. Sidebar ẩn <1024px (pattern S9.x). |

## Kiểm chứng đã chạy

- **FE `npx eslint`** (8 file S11.1: dashboard, chart, cards, layout, sidebar, App, analyticsService) → **0 lỗi**.
- **BE `python -m py_compile`** → pass: `routers/admin_analytics.py`, `repositories/analytics_repo.py`, `schemas/analytics.py`.
- **Router wiring** — `main.py` include `admin_analytics.router`; 2 endpoint khớp plan.
- **Auth** — `require_admin` kiểm `role=='admin'`; log INFO chỉ ghi `admin_id`, không log số liệu (§4.5).
- **Route FE** — `/admin` → `/admin/dashboard`; `AdminLayout` title map "Dashboard".

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa
- **Smoke PostgreSQL E2E** chưa chạy trong session review — cần login admin + gọi overview/timeseries trên DB có seed.
- **`getOverview` / `getTimeseries` qua `apiClient`** — chưa refresh token khi 401 (cùng pattern S9.2/S10.2; edge case hiếm).
- **Doanh thu `is_estimated=true`** — đúng scope S11.1; chuyển sang `payment_transaction` thuộc S11.2.

### 🟢 Quan sát thêm (ngoài phạm vi)
- Nav **Reports** → `/admin/reports` chưa có route (S11.3).
- **Analytics** sidebar trùng path dashboard — đã xử lý không double-active Overview.
- **NFR §4.1 latency** — đo thực tế khi deploy/staging.

## Kết luận

Story S11.1 **PASS**: dashboard admin end-to-end khớp plan + AC-12-01 (phần overview §3.6), BE bảo vệ admin, FE bám mockup `dashboard_quan_tri_desktop` với recharts và empty state an toàn. AI usage placeholder là thiết kế đã chốt trong plan. Không blocker — nợ nhỏ: smoke DB thủ công và apiClient 401 refresh (tùy chọn).
