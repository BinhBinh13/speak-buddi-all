# Review — S9.4: Fallback Langeek lỗi — cache / retry / log / notify Admin

- **Ngày:** 2026-06-07
- **Verdict:** PASS

## Đối chiếu Acceptance Criteria

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-13-05 — giữ cache, retry, log, notify khi crawl fail | ✅ đạt | `_handle_failure()` không gọi `publish_batch`; `append_log` payload có URL/reason/cache; `schedule_next_retry`; `notify_crawler_failure` + email §11.6. |
| BR12 — nội dung active vẫn available cho học viên | ✅ đạt (logic) | Failure path early return trước publish/soft-disable; `compute_cache_active` dựa `last_success_at` + đếm `topic_word` active. Learner API không đổi. |
| FR-20 — cache + retry + log + notify | ✅ đạt | Scheduler interval 3 phút (`run_pending_retries` + stale cleanup); sync-status mở rộng; Admin banner + retry thủ công. |
| §11.6 SRS — email/banner fields | ✅ đạt | `send_crawler_failure_email` gồm source URL, reason, last success, retry status, cache status, link `/admin/crawler`. |
| S3.4 — không publish/disable hàng loạt khi fail | ✅ đạt | `publish_batch` chỉ trong success path; không gọi `soft_disable_langeek_words_not_in_batch` trong `_handle_failure`. |

## Kiểm chứng đã chạy

- **BE `python -m py_compile`** → pass: `crawler_job`, `crawler_retry`, `crawl_repo`, `admin_crawler`, `admin_notification_service`, `email_service`, `crawler_scheduler`, `schemas/crawler`.
- **BE smoke `should_retry`** → boundary 1/3/4 với `max_retries=3` — pass.
- **FE `npm run lint`** → 0 lỗi mới trong `features/admin/crawler/**`; 7 lỗi pre-existing RoadmapSection/RoadmapNode.
- **Code review** — API đủ plan: sync-status mở rộng, `GET /jobs/{id}/logs`, `POST /jobs/{id}/retry`, PUT config retry fields, `simulate_fail`, scraper `--fail`.

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa (không chặn PASS)
- **Smoke test PostgreSQL E2E** chưa chạy trong session review (cần `schema_crawler_s9_4.sql` + `POST jobs/run {simulate_fail:true}` + verify `topic_word` count không đổi).
- **Unit tests** theo plan (`compute_cache_active`, file test) chưa có — chỉ smoke thủ công `should_retry`.
- **Auto-retry success:** job failed cũ có thể giữ `retry_status=pending` sau khi retry job mới thành công — sync-status có thể hiển thị retry cũ cho đến job failed kế tiếp; cân nhắc reset status job gốc khi retry thành công.
- **Stale running cleanup** mark failed + schedule retry nhưng không gọi `notify_crawler_failure` (edge case hiếm).
- **FE chưa dùng `getJobLogs`** — API có, UI vẫn bảng job tóm tắt (chấp nhận MVP).

### 🟢 Quan sát thêm (ngoài phạm vi)
- Admin chỉnh/disable nội dung crawl → **S9.5**.
- Cấu hình retry/notify email trên UI Admin (BE PUT `/config` đã có).

## Kết luận

Story S9.4 **PASS**: luồng lỗi crawler giữ cache active, lên lịch retry, ghi log chi tiết, thông báo Admin (email + banner FE), tuân hợp đồng S3.4. Có `--fail` / `simulate_fail` để test không cần Langeek thật. Không blocker; nợ nhỏ: smoke DB runtime + polish retry status sau success.
