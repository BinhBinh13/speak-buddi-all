# Review — S6.2: Chấm phát âm + hiển thị feedback/score

- **Ngày:** 2026-06-07
- **Verdict:** PASS
- **Lần review:** 2 (sau khi sửa CHANGES_REQUESTED lần 1)

## Đối chiếu Acceptance Criteria

AC nguồn (SRS §UC08): **AC-08-03** — *Given pronunciation scoring succeeds, when feedback is returned, then the system displays score/feedback and stores metadata/score.*

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-08-03 — Điểm tổng (gauge 0–100) | ✅ đạt | `ScoreGauge` render điểm overall; clamp `[0,100]` 3 lớp (FE Field, Pydantic `ge/le`, service `_clamp`) |
| AC-08-03 — Sub-score accuracy/fluency + progress bar | ✅ đạt | `SubScoreCard` cho Chính xác/Lưu loát, width = value%; khớp mockup |
| AC-08-03 — Breakdown âm tiết tô màu | ✅ đạt | `SyllableChips`: ≥75 → secondary-container `#6cf8bb`/`#00714d`, <75 → tertiary `#ffb695`/`#7b2f00` — đúng mockup (De/par xanh, ture cam) |
| AC-08-03 — Feedback tiếng Việt | ✅ đạt | Prompt yêu cầu feedback tiếng Việt ≤2 câu; `FeedbackBox` render `{feedback}` thuần (an toàn — xem mục Fix) |
| AC-08-03 — Lưu metadata/score vào DB | ✅ đạt | Router lưu `pronunciation_attempt` + N `pronunciation_syllable_score` trong 1 transaction (commit/rollback), trả `attempt_id` |
| Xử lý lỗi scoring (400/502/500/401) | ✅ đạt | 400 transcript rỗng; 502 AI lỗi; 500 DB lỗi (rollback); 401 thiếu JWT. FE map message §5.2 qua `err.status` |
| Auth guard | ✅ đạt | `Depends(current_user)` → thiếu/sai JWT → 401 |
| Nút Thử lại / Từ tiếp theo | ✅ đạt | `handleReset` về idle; `handleNext` disable khi hết từ (opacity 0.5, cursor not-allowed) |
| UI bám mockup + responsive + touch ≥44px | ✅ đạt | Bento flex-wrap (3 cột desktop → 1 cột mobile); 2 nút `minHeight:44` |
| Không log audio/transcript/secret (§4.5) | ✅ đạt | Service log `target` + `len(transcript)`; repo log `target` + `attempt_id`; không log token/transcript đầy đủ |

## Kiểm chứng đã chạy

- `cd speak-buddi && npm run lint` → **8 errors, 0 warnings** — TẤT CẢ nằm ngoài phạm vi S6.2 (giống lần 1):
  - `dashboard/components/RoadmapSection.jsx` (5): `CR`/`isCurrent`/`totalXP`/`progress` unused + `useNavigate` gọi điều kiện
  - `roadmap/components/RoadmapNode.jsx` (2): `ON_SURFACE`, `OUTLINE_VARIANT` unused
  - `roadmap/components/TopicModal.jsx` (1): `handleLearnVocab` unused
  - **0 lỗi trong file pronunciation của S6.2.**
- Backend: không chạy runtime (cần JWT + ANTHROPIC_API_KEY + Postgres). Kiểm chứng bằng đọc code: luồng 400/502/500/401, transaction rollback, clamp, parse phòng thủ, không log nhạy cảm — hợp lệ.
- Migration `007_pronunciation.sql`: idempotent (`IF NOT EXISTS`), UUID PK, CHECK `BETWEEN 0 AND 100`, FK `users`/`topic_word`, `audio_url` NULL theo plan — SQL hợp lệ về cú pháp.

## Xác nhận sửa CHANGES_REQUESTED lần 1

| # | Mức cũ | File | Trạng thái | Bằng chứng |
|---|---|---|---|---|
| 1 | 🔴 Blocker XSS | `FeedbackBox.jsx:15` | ✅ ĐÃ SỬA | Đã bỏ `dangerouslySetInnerHTML`, render `<p>{feedback}</p>` thuần — React tự escape, không còn đường XSS từ LLM output |
| 2 | 🟡 | `shared/api/client.js:66` | ✅ ĐÃ SỬA | Thêm `error.status = res.status` vào Error trước khi throw |
| 3 | 🟡 | `PronunciationPage.jsx:96` | ✅ ĐÃ SỬA | Đổi sang `if (err?.status === 400)` — dựa mã lỗi thật thay vì so chuỗi |
| 4 | 🟡 | `pronunciation_service.py:76-79` | ✅ ĐÃ SỬA | `re.search(r"\{.*\}", raw, re.DOTALL)` trích `{...}` trước `json.loads`; raise `ValueError` rõ ràng khi không tìm thấy JSON → caller map 502 |

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không còn. Blocker XSS lần 1 đã được xử lý đúng cách.

### 🟡 Nên sửa
- Không có blocker. Các điểm 🟡 lần 1 đều đã được khắc phục.

### 🟢 Quan sát thêm (ngoài phạm vi)
- 8 lint error pre-existing ở `dashboard`/`roadmap` không thuộc S6.2 — cần story riêng dọn. Đặc biệt `useNavigate` gọi điều kiện (`RoadmapSection.jsx:92`) là vi phạm rules-of-hooks thật sự, dễ gây bug runtime.
- `SubScoreCard` dùng ký tự unicode thay Material Symbols trong mockup (FE không nạp Material Icons) — lệch nhẹ, chấp nhận được.
- Approach scoring dựa transcript (SpeechRecognition) không phải acoustic/phoneme-level — đã ghi TBD trong plan, hợp lý cho v1.1.0.
- `topic_word_id` luôn NULL (từ hardcode) — đúng phạm vi, chờ story nối nguồn từ thật.
- `audio_url` NULL — đúng phạm vi (S6.3 lo lưu audio + chính sách 30 ngày).

## Kết luận

Cả 4 điểm CHANGES_REQUESTED lần 1 đã được sửa đúng và đầy đủ — quan trọng nhất là blocker XSS ở `FeedbackBox` (giờ render text an toàn). Toàn bộ AC-08-03 đạt: chấm + hiển thị (gauge/sub-score/chip/feedback) + lưu DB (attempt + syllable trong 1 transaction); error handling 400/502/500/401 đầy đủ và FE map message §5.2 qua `err.status` tin cậy; không log audio/transcript/secret; migration idempotent; lint sạch trong phạm vi story (8 lỗi còn lại đều pre-existing, ngoài phạm vi). Không còn blocker → **PASS**, story chuyển `done`.
