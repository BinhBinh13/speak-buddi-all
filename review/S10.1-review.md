# Review — S10.1: CRUD payment plan + validation

- **Ngày:** 2026-06-07
- **Verdict:** PASS

## Đối chiếu Acceptance Criteria

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-14-01 — tạo/sửa gói hợp lệ → lưu | ✅ đạt | `POST/PUT /api/admin/payment-plans` + repo `INSERT/UPDATE ... RETURNING`; FE toast + refresh list sau create/update. |
| AC-14-02 — giá sai / thiếu field / 0 feature → inline error | ✅ đạt | FE `validatePaymentPlanForm` + `FieldError`/`is-invalid`; BE `_validate_create/_validate_update` → 400 + message `⚠`; `mapPaymentPlanApiErrorToFields` map lỗi BE. |
| Validation giá ≥ 0, gói trả phí > 0 | ✅ đạt | Pydantic `ge=0` + BE check âm; FE checkbox "Gói miễn phí" cho phép 0, gói trả phí bắt `price > 0` trên form. |
| ≥ 1 feature | ✅ đạt | FE + BE lọc whitespace, chặn mảng rỗng. |
| BR07 — chỉ admin | ✅ đạt | Router `dependencies=[Depends(require_admin)]`; FE route dưới `AdminRoute` + `AdminLayout`; sidebar nav `/admin/payments`. |
| §5.2 — message tiếng Việt, prefix `⚠`, inline | ✅ đạt | Message thống nhất trong `adminValidation.js` và `admin_payment_plan.py`. |
| NFR §4.8 responsive | ✅ đạt | Grid 1/2/3 cột; nút `min-height: 44px`; modal Bootstrap centered. |
| Soft delete (S10.2) | ⏭ ngoài scope | `is_active` + Update nhận field; không UI disable — đúng plan. |

## Kiểm chứng đã chạy

- **FE `npx eslint`** (file S10.1) → **0 lỗi** trong `payment-plans/**`, `adminValidation.js`, `App.jsx`, `AdminLayout.jsx`.
- **FE `npm run lint` toàn project** → 6 lỗi pre-existing (`RoadmapSection.jsx`, `RoadmapNode.jsx`); không thuộc S10.1.
- **BE `python -m py_compile`** → pass: `payment_plan_admin.py`, `payment_plan_admin_repo.py`, `admin_payment_plan.py`.
- **BE import router** → `from routers import admin_payment_plan` OK; `main.py` đã `include_router`.
- **Đối chiếu code vs plan** → đủ 4 endpoint, route `/admin/payments`, tái dùng `AdminLayout`/`AdminSidebar`, mockup `quan_li_goi_thanh_toan_admin` (grid card + Create/Edit).
- **Smoke PostgreSQL E2E** → chưa chạy trong session review (cần DB + token admin).

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa
- **BE không bắt `price_vnd > 0` cho gói trả phí** — chỉ FE form; gọi API trực tiếp vẫn tạo gói trả phí giá 0. Chấp nhận theo quyết định plan (gói Miễn phí seed sẵn); có thể harden BE sau nếu cần.
- **`GET /api/admin/payment-plans/{id}`** có nhưng FE edit dùng data từ list — không sai, chỉ dư endpoint cho form tương lai.

### 🟢 Quan sát thêm (ngoài phạm vi)
- Mockup có 3 metric card (MRR/Subscribers/Conversion) — plan loại khỏi S10.1 (Epic 11); đúng scope.
- `apiClient` parse `err.detail` chuỗi — khớp BE `HTTPException(detail=...)`.
- Smoke runtime PostgreSQL + so sánh visual `screen.png` nên làm thủ công trước production.

## Kết luận

Story S10.1 **PASS**: CRUD Admin payment plan end-to-end (BE 4 endpoint + FE `/admin/payments`) với validation inline AC-14-02, phân quyền admin BR07, bám mockup và pattern S9.1. Không blocker. Nợ nhỏ: harden BE rule giá trả phí > 0 (tùy chọn) và smoke DB thủ công.
