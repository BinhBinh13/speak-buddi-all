# Review — S1.3: Chặn Guest truy cập tính năng học → redirect login

- **Ngày:** 2026-06-06
- **Verdict:** PASS

## Đối chiếu Acceptance Criteria

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-01-03 (Guest → redirect `/login?next=`, không flash) | ✅ đạt | `ProtectedRoute.jsx` render `<Navigate to={\`/login?next=${next}\`} replace />` ngay trong render pass (không dùng `useEffect`) → không render `<Outlet/>`, không flash nội dung bảo vệ. `next` = `encodeURIComponent(location.pathname + location.search)`. Route `/dashboard`, `/speaking` nằm trong nhóm `<Route element={<ProtectedRoute/>}>` (App.jsx:32-34). |
| BR01 (route public render khi chưa login) | ✅ đạt | Tất cả 9 route public (`/`, `/pricing`, `/login`, `/register`, `/oauth/callback`, `/forgot-password`, `/reset-password`, `/privacy`, `/terms`) khai báo NGOÀI guard trong App.jsx:21-29, không bị `ProtectedRoute` chặn. Khớp đúng danh sách yêu cầu. |
| AC-04-03 (liên đới, roadmap) | ⚠️ chưa kiểm chứng (đúng phạm vi) | `/roadmap` đang ở dạng comment skeleton bên trong nhóm guard (App.jsx:41). Khi page thật ra đời (story khác) nó sẽ tự được bảo vệ. Chấp nhận vì route chưa tồn tại — không thuộc phạm vi hiện thực S1.3. |
| `?next=` flow (redirect đúng đích sau login) | ✅ đạt | `LoginPage.getSafeNext()` (dòng 191-204) đọc `?next=`, decode, navigate `{ replace: true }`. `OAuthCallbackPage` (dòng 20-33) cùng logic. Cả hai gọi `login(token)` từ context → guard cập nhật ngay. |
| Open-redirect bị chặn (URL chứa `://` reject) | ✅ đạt | `getSafeNext()` chỉ chấp nhận `decoded.startsWith("/") && !decoded.includes("://")`, fallback `/dashboard`. `try/catch` quanh `decodeURIComponent` chống chuỗi malformed (URIError). OAuthCallback áp dụng y hệt. `/login?next=https://evil.com` → fallback `/dashboard`. |

## Kiểm chứng đã chạy

- `cd speak-buddi && npm run lint` → **10 errors, 0 warnings** — đúng bằng baseline pre-existing ghi trong implement log.
  - 2 lỗi `LoginPage.jsx` (`FONTS`, `GOOGLE_FONTS_URL` unused, dòng 2) là import **có sẵn từ trước**, không phải do S1.3. S1.3 chỉ thêm import dòng 3 (`useNavigate`, `useSearchParams`) và dòng 5 (`useAuth`) — tất cả đều được dùng, không sinh lỗi.
  - 8 lỗi còn lại thuộc `RoadmapSection.jsx`, `SpeakingPage.jsx`, `speechService.js` — pre-existing, ngoài phạm vi S1.3.
- **6 file thuộc S1.3 (AuthContext, ProtectedRoute, App.jsx, OAuthCallbackPage, PublicNavbar, + thay đổi LoginPage) đều lint sạch** — 0 lỗi mới. Lỗi `react-refresh/only-export-components` trên `useAuth` đã được disable hợp lý (hook + provider chung file).

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa
- Không có trong phạm vi story. (Các điểm dưới là quan sát.)

### 🟢 Quan sát thêm (ngoài phạm vi)
- **Guard chỉ check token tồn tại**, không verify `exp`/chữ ký. Token giả/hết hạn vẫn qua guard FE; lớp chặn thực là `apiClient` (401 → hard redirect). Đúng như plan §9 ghi nhận, defense-in-depth ở BE/refresh-token thuộc S1.5. Chấp nhận cho v1.
- **`apiClient.js` dùng `window.location.href` (hard redirect)** khi 401 — phá trải nghiệm SPA và không qua `useAuth().logout()`. Plan §6 đã ghi nhận là nợ kỹ thuật, refactor sau.
- **Placeholder routes** (`/register`→LoginPage, `/privacy`/`/terms`→LandingPage) chỉ là tạm để redirect không trỏ route trống; sẽ thay bằng page thật ở S1.4/S1.7. Không ảnh hưởng AC.
- **`PublicNavbar.APP_ROUTES`** liệt kê `/schedule`, `/vocabulary`, `/analytics` chưa khai báo route — chỉ ảnh hưởng logic hiển thị navbar, không liên quan guard. Ngoài phạm vi.
- **`authService.isAuthenticated()`** vẫn tồn tại song song với `useAuth().isAuthenticated`. Hiện không gây xung đột (cùng nguồn `localStorage`), nhưng nên thống nhất về context ở các story sau để tránh trạng thái lệch.

## Kết luận

S1.3 đạt PASS. Cơ chế guard đúng chuẩn react-router v7: `<ProtectedRoute>` redirect ngay trong render pass (không `useEffect`) nên không flash nội dung bảo vệ — thỏa AC-01-03. Route public tách bạch ngoài guard (BR01 đạt), `?next=` flow hoạt động với whitelist same-origin chống open-redirect ở cả LoginPage và OAuthCallback (reject `://`, try/catch chống malformed URI). Lint giữ nguyên baseline 10 lỗi pre-existing, không sinh lỗi mới. AC-04-03 chưa kiểm chứng được vì `/roadmap` chưa có page thật, nhưng đã đặt trong nhóm guard nên sẽ tự bảo vệ — đúng phạm vi story. Không có blocker → story chuyển `done`.
