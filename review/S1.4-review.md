# Review — S1.4: Đăng ký bằng email/password + validation

- **Ngày:** 2026-06-06
- **Verdict:** PASS
- **Loại:** Re-review sau lint fixes post-implement (story đã PASS lần trước)

## Đối chiếu Acceptance Criteria (AC-02-01, §5.2)

| AC | Kết quả | Ghi chú |
|---|---|---|
| Form đủ 4 fields (họ tên, email, mật khẩu, xác nhận) | ✅ đạt | `RegisterPage.jsx`: name (L431), email (L445), password (L486), confirm (L568). |
| Email format hợp lệ (client) | ✅ đạt | FE regex L278 `^[^@\s]+@[^@\s]+\.[^@\s]+$`; BE `_validate_email` main.py L287-289. |
| Password ≥ 8 ký tự + ≥1 số (client) | ✅ đạt | FE L281 `password.length < 8 \|\| !/\d/`; BE `_validate_password` L291-293. Khớp §5.2. |
| Confirm khớp | ✅ đạt | FE L284 `password !== confirm` → `Mật khẩu xác nhận không khớp.` |
| Empty fields | ✅ đạt | FE L275 + BE L303 → `Vui lòng điền đầy đủ thông tin.` |
| Password strength bar | ✅ đạt | `PasswordStrengthBar` L209-258 + `calcStrength` L34 (length/digit/special); màu emerald cho mạnh. |
| Toggle show/hide | ✅ đạt | Toggle riêng cho password (L515) và confirm (L597); `aria-label`, touch target ≥44px. |
| BE `POST /api/auth/register` | ✅ đạt | main.py L296-350: validate → tạo user role=student, hash sha256, ký JWT exp +7 ngày. |
| Chống trùng email (409) | ✅ đạt | L316-317 → 409 `Email đã được sử dụng.`; email normalize lowercase trước check. |
| Trả JWT + shape giống login | ✅ đạt | Return `{token, user{id,name,email,level,streak,goal}}` (L340-350) khớp `/api/auth/login`. |
| Auto-login (`AuthContext.login`) | ✅ đạt | `handleRegister` L301-304: `register(...)` → `login(data.token)` → `navigate("/dashboard")`. |
| Không log password/token | ✅ đạt | BE chỉ log `id` + `email` (L331); password lưu hash sha256 (L325); FE không `console.log`. |

## Kiểm chứng đã chạy
- `npm run lint` (toàn codebase) → **5 errors, 0 warnings**, TẤT CẢ trong `src/features/dashboard/components/RoadmapSection.jsx` (ngoài phạm vi S1.4): unused vars `CR`/`isCurrent`/`totalXP`/`progress` + `useNavigate` gọi có điều kiện (`react-hooks/rules-of-hooks`). Không có lỗi nào trong file thuộc S1.4.
- `npx eslint` trên RegisterPage.jsx, App.jsx, authService.js, apiClient.js, **LoginPage.jsx** → **0 error, 0 warning**. LoginPage (file bị chỉnh lint fix lần này) đã sạch.
- `python -m py_compile main.py` → **Syntax OK**.

> So với implement log (ghi 10 lỗi pre-existing ở LoginPage/RoadmapSection/SpeakingPage/speechService): sau lint fixes, LoginPage + SpeakingPage + speechService đã sạch; chỉ còn 5 lỗi ở RoadmapSection. Codebase sạch hơn lần review trước, không phát sinh lỗi mới do thay đổi liên quan S1.4.

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa
- **Thiếu prefix `⚠` trong message:** §5.2 quy định dạng `⚠ Email không hợp lệ.` nhưng FE (`validateForm`) và BE (`detail`) dùng chuỗi không có `⚠`. FE bù bằng `IconError` SVG cạnh text (UX chấp nhận được), nhưng message BE hiển thị trực tiếp (vd 409 `Email đã được sử dụng.`) không có dấu cảnh báo — nên thống nhất để khớp spec chữ.
- **`hasError` chỉ tô viền đỏ field email:** email dùng `hasError={error.toLowerCase().includes("email")}` (L453); password/confirm không nhận viền lỗi. Minor thị giác, không chặn chức năng.

### 🟢 Quan sát thêm (ngoài phạm vi)
- `RoadmapSection.jsx` còn 5 lỗi lint (dashboard, ngoài S1.4). Lưu ý `react-hooks/rules-of-hooks` (`useNavigate` gọi có điều kiện, L92) là bug thực sự — nên xử lý ở story dashboard tương ứng.
- `apiClient.js` (L16-20): khi 401 redirect cứng `/login` + return undefined — không ảnh hưởng S1.4 (register dùng 400/409), nhưng cân nhắc khi làm S1.5.
- `MOCK_USERS` RAM-only mất khi restart BE — nợ kỹ thuật đã ghi nhận, chờ DB thật (S1.1 mở rộng).
- Redirect sau đăng ký về `/dashboard` thay vì `/onboarding` (S2.1 chưa có route) — đã comment trong code.
- BE thiếu giới hạn độ dài `name`/`password` — cân nhắc khi có DB thật.

## Kết luận
Toàn bộ AC của AC-02-01 + message §5.2 liên quan đăng ký vẫn đạt sau lần chỉnh sửa lint: validation 2 lớp (FE + BE) nhất quán, chống trùng email 409, auto-login + redirect hoạt động, không log secret, password lưu hash. Lint trên mọi file thuộc S1.4 (kể cả LoginPage vừa fix) hoàn toàn sạch; 5 lỗi còn lại nằm ngoài phạm vi (RoadmapSection). BE compile OK. Không có blocker → **PASS**, giữ story ở `done`.
