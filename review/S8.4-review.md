# Review — S8.4: Paid User chọn/đổi voice/model ElevenLabs; chặn free user

- **Ngày:** 2026-06-07
- **Verdict:** PASS

## Đối chiếu Acceptance Criteria

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-11-01 — Paid User xem danh sách voice active | ✅ đạt | `GET /api/voice/models` (`routers/voice.py` line 35–43) dùng `Depends(require_paid)` + `voice_repo.list_active_voice_models` lọc `is_active=TRUE`. FE `VoiceSettingsPage` gọi `getVoiceModels()` và render lưới thẻ voice. |
| AC-11-02 — chọn + lưu preference, persist reload | ✅ đạt | `PUT /api/voice/preference` validate voice active → UPSERT `user_voice_preference` (`voice_repo.set_user_preference`). FE chọn thẻ → "Lưu thay đổi" → `setVoicePreference()`. `GET /api/voice/preference` highlight lại sau reload. `/speak` + `/tts` (`routers/ai.py`) gọi `get_voice_id_for_user` → truyền vào `text_to_audio_bytes(voice_id=..., model_id=...)`. |
| AC-11-03 — Free Student bị từ chối + mời nâng cấp | ✅ đạt | BE: `require_paid` → 403 `"Tính năng này yêu cầu gói Pro."` (`auth/deps.py` line 28–31). FE: route `/settings/voice` bọc `PaidRoute` → free user redirect `/pricing` (`App.jsx` line 101–103). Trang còn xử lý 403 từ API → banner + link `/pricing` (phòng JWT `is_paid` lệch). |
| BR06 — chỉ Paid User đổi được (enforce BE) | ✅ đạt | Cả 3 endpoint voice đều `Depends(require_paid)` — không bypass được qua UI ẩn. |
| §4.5 — không log secret; audit định danh | ✅ đạt | `set_preference` chỉ log `VOICE_PREF_SET user=%s voice_model=%s` (không API key/audio). TTS log giữ `text=%r` truncated như cũ. |
| §4.8 — responsive, touch ≥44px | ✅ đạt | Grid `repeat(auto-fill, minmax(220px, 1fr))`; nút Lưu/Đặt lại `minHeight: 44`; dùng `AppLayout` + design tokens `UI.*` khớp DESIGN.md. |
| Backward-compat TTS (chưa chọn voice) | ✅ đạt | `get_voice_id_for_user` trả `None, None` → `tts_service` fallback `ELEVENLABS_VOICE_ID` / `eleven_multilingual_v2`. `optional_current_user` cho phép `/tts`/`/speak` không JWT như trước. |

## Kiểm chứng đã chạy

- **FE `npm run lint`** → 7 errors, tất cả pre-existing ở `RoadmapSection.jsx` (5) + `RoadmapNode.jsx` (2). 0 lỗi trong `VoiceSettingsPage.jsx`, `voiceService.js`, `App.jsx`, `AppLayout.jsx`.
- **BE syntax** (`ast.parse` 6 file S8.4) → OK.
- **Route** — `App.jsx` line 103: `/settings/voice` trong `<PaidRoute>`, import `VoiceSettingsPage` line 31.
- **Router đăng ký** — `main.py` `include_router(voice.router)`.
- **Schema** — `schema_voice.sql` idempotent, FK `users`, trigger `set_updated_at`; seed 3 voice khớp default `pNInz6obpgDQGcFmaJgB`.
- **Mockup** — đối chiếu `cai_dat_giong_doc_desktop`: header Việt hóa, section "Giọng đọc", lưới thẻ + badge PRO (gold gradient), badge "Đang chọn", footer Lưu/Đặt lại. Speaking Rate + Auto-Play **ẩn** đúng plan (ngoài AC).

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa
- **`voice_repo.set_user_preference` gọi `db.commit()` trong repo** trong khi route dùng `Depends(get_db)` (cũng commit ở cuối) — pattern lệch so với `payment_repo` (không commit trong repo). Hiện vẫn hoạt động; nên thống nhất sau để tránh double-commit khó debug.
- **`/speak`/`/tts` phụ thuộc bảng voice** — nếu chưa chạy `schema_voice.sql`, query `get_voice_id_for_user` có thể 500 thay vì fallback default. Plan giả định migration đã chạy; có thể bọc try/except fallback cho hardening trước production.
- **`genderIcon`** — male/female cùng icon `LuUser`; không ảnh hưởng AC, chỉ UI nit.
- **Sidebar** vẫn link `/settings` chung, chưa có link trực tiếp `/settings/voice` — plan ghi tùy chọn, không chặn PASS.

### 🟢 Quan sát thêm (ngoài phạm vi)
- Preview audio từng voice, Speaking Rate, Auto-Play: chưa làm / ẩn — đúng plan.
- Admin CRUD `elevenlabs_voice_model`: seed cứng, story Admin sau.
- Chưa có pytest/DB test tự động; smoke E2E cần chạy thủ công sau `schema_voice.sql` + seed.

## Kết luận

PASS. AC-11-01/02/03 và BR06 đều đạt: Paid User xem/lưu voice qua API + UI; free user bị chặn ở BE (403) và FE (`PaidRoute` → `/pricing`); TTS áp preference khi có, fallback default an toàn. Code khớp plan, mockup Việt hóa, audit §4.5 đúng. Chỉ có vài điểm hardening/pattern nhỏ ở mức "nên sửa", không chặn merge → story chuyển `done`. EPIC-8 hoàn tất 4/4 story.
