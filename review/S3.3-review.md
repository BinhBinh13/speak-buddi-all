# Review — S3.3: Lưu tiến độ học từ/topic

- **Ngày:** 2026-06-06
- **Verdict:** PASS
- **Lượt review:** 2 (re-review sau fix `MOCK_USERS` id → UUID)

## Đối chiếu Acceptance Criteria

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-05-03 (per-word progress) | ✅ đạt | Bảng `user_word_progress` lưu `status` (known/learning) cho `(user_id, topic_word_id)` với `UNIQUE (user_id, topic_word_id)`. `PUT .../progress` upsert qua `ON CONFLICT (user_id, topic_word_id) DO UPDATE` — không tạo row trùng, `review_count = user_word_progress.review_count + 1`. |
| AC-05-03 (level + topic context) | ✅ đạt | Mỗi row lưu snapshot `topic_id` (NOT NULL) + `level_id` (nullable). PUT lấy `level_id` từ `topic_word` tại thời điểm lưu — đúng cụm "with level and topic context". |
| AC-05-03 (idempotent upsert) | ✅ đạt | `INSERT ... ON CONFLICT` đúng key; lưu lại cùng từ → 1 row, status cập nhật, `last_seen_at`/`updated_at` = NOW(). |
| AC-05-03 (GET tổng hợp topic) | ✅ đạt | `GET .../progress` trả `total_words`, `known_count`, `learning_count`, `percent_known`, `words[]`. 404 khi topic inactive/không tồn tại (đồng nhất `get_topic_words`). |
| AC-05-03 (FE lưu + hydrate + progress bar) | ✅ đạt | 2 nút "Đã thuộc"/"Cần luyện thêm" gọi `saveWordProgress` (PUT, `method:"PUT"` + `body` JSON) optimistic + rollback; `handleSelectTopic` hydrate `progressMap`/`knownCount` qua `getTopicProgress`; progress bar known-based. Badge trạng thái trên Flashcard. |
| Security (NFR) | ✅ đạt | Cả 2 endpoint `Depends(current_user)`; query luôn lọc `user_id` từ token → user A không thấy progress user B. Log chỉ ghi user_id/word/status, không ghi token/secret. |

## Kiểm chứng đã chạy (lượt 2)

- `python -m py_compile routers/learning.py schemas/learning.py` (từ `speak-buddi-be/`) → **PY_COMPILE_OK**.
- Xác nhận fix: `main.py:124` — `MOCK_USERS["demo@speakbuddi.com"]["id"] = "00000000-0000-0000-0000-000000000002"` (UUID hợp lệ).
- Xác nhận `routers/learning.py:162` và `:237` — `uuid_mod.UUID(payload["sub"])` parse UUID demo không raise `ValueError` → không còn HTTP 500 cho user demo. Đây là đường verify chính thức của story.
- Kiểm tra các file S3.3 khác không bị phá vỡ:
  - `db/schema_progress.sql` — bảng + 3 index + trigger, idempotent (`IF NOT EXISTS`/`CREATE OR REPLACE TRIGGER`). Không đổi.
  - `schemas/learning.py` — `WordProgressUpsert`/`WordProgressOut`/`TopicProgressOut` + `Literal` import. Compile OK.
  - `vocabularyService.js` — `saveWordProgress` (`method:"PUT"` + `body`) + `getTopicProgress` đúng. Không đổi.
  - `VocabularyPage.jsx` / `Flashcard.jsx` — logic hydrate + badge giữ nguyên (lượt 1 đã lint sạch).

## Phát hiện

### 🔴 Phải sửa (blocker)
- (không có)

### 🟡 Nên sửa
- **[main.py:358]** User **đăng ký mới** (S1.4) vẫn nhận `id = f"u_{...:03d}"` (vd `u_002`) — không phải UUID. Nếu user mới này gọi progress endpoint, `uuid_mod.UUID(payload["sub"])` vẫn raise `ValueError` → HTTP 500. Fix lần này chỉ xử lý user **demo** (đường verify chính thức của S3.3). Với auth còn là mock và demo là user duy nhất có DB row seed, đây là nợ kỹ thuật chấp nhận được — cần xử lý khi auth thật/registered user được DB-provision (S1.x / lazy-provision như Plan §6 từng đề xuất). Không chặn PASS.
- **[routers/learning.py:278 vs VocabularyPage.jsx]** `percent_known` backend `round(..., 1)` còn FE tự tính `Math.round` (số nguyên). Không gây bug (FE không dùng giá trị % của backend cho thanh), nhưng nên thống nhất định dạng để tránh nhầm lẫn sau này.

### 🟢 Quan sát thêm (ngoài phạm vi)
- `RoadmapSection.jsx` còn 5 lỗi lint pre-existing (gồm 1 `react-hooks/rules-of-hooks`) — file ngoài phạm vi S3.3, xử lý ở story dashboard riêng.
- Schema dùng `ON DELETE CASCADE` cho `topic_word_id`/`topic_id`: hard-delete sẽ xoá progress; phù hợp thiết kế soft-delete (`is_active=false`) và chuẩn bị tốt cho S3.4 (AC-05-04).

## Kết luận
Fix `MOCK_USERS["demo@speakbuddi.com"]["id"]` → UUID hợp lệ đã được xác nhận trong `main.py`; `uuid_mod.UUID(payload["sub"])` ở cả 2 endpoint không còn crash với user demo. Compile BE OK, các file S3.3 khác (schema, schemas, service, page, flashcard) nguyên vẹn và đúng spec. AC-05-03 đạt đầy đủ trên đường verify chính thức (user demo). Verdict **PASS**. Còn 1 nợ kỹ thuật 🟡 cho registered user mới (id chưa phải UUID) — không chặn vì auth còn mock; ghi lại để xử lý khi có DB-provision/lazy-provision.
