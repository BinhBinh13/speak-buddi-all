# Review — S11.2: Lọc doanh thu theo ngày/tháng/năm/gói

- **Ngày:** 2026-06-07
- **Verdict:** PASS

## Đối chiếu Acceptance Criteria

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-12-02 — Admin lọc doanh thu theo date/plan → hiển thị khớp | ✅ đạt | `GET /revenue` + `RevenueFilterPanel` (preset/from-to/plan) + summary; chart revenue gọi cùng filter qua `getRevenueFiltered`. |
| §3.6 — revenue theo ngày/tháng/năm/tổng + lọc theo gói | ✅ đạt | Preset `day|month|year|total`; custom `from`/`to`; `plan_id` optional; bucket points theo granularity. |
| Admin-only | ✅ đạt | `require_admin` trên `/revenue` và `/timeseries`; FE dưới `AdminRoute`. |
| Validation §5.2 | ✅ đạt | BE: thiếu 1 trong 2 ngày → 400; `from > to` → 400; plan không tồn tại → 404. FE: disable Áp dụng + message inline. |
| Zero state | ✅ đạt | Không có tx success → `total_vnd=0`, "Chưa có giao dịch" trên chart/summary. |
| Nguồn doanh thu thật | ✅ đạt | `payment_transaction` (`status='success'`, `amount_vnd`, `paid_at`); `is_estimated=false`; overview KPI đồng bộ. |
| §4.5 logging | ✅ đạt | Log chỉ `admin_id`, `granularity`, `has_plan`, `custom_range` — không log amount. |

## Kiểm chứng đã chạy

- **FE `npx eslint`** (4 file S11.2) → **0 lỗi**.
- **BE `python -m py_compile`** → pass: `admin_analytics.py`, `analytics_repo.py`, `analytics.py`.
- **Router** — `/revenue` map query `from`/`to` đúng alias; `/timeseries?plan_id=` chỉ áp revenue.
- **Repo** — `get_revenue_stats`, `get_revenue_filtered`, `get_timeseries(revenue)` đều query `payment_transaction`.

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa
- **Smoke PostgreSQL E2E** chưa chạy — cần migrate `schema_payment.sql` + seed giao dịch success để demo số khác 0.
- **`getRevenueFiltered` / `apiClient`** — chưa refresh token 401 (pattern S11.1/S10.2; edge case hiếm).
- **Preset timezone** theo `NOW()` server DB — ghi chú nếu PO cần TZ Việt Nam.

### 🟢 Quan sát thêm (ngoài phạm vi)
- Chart users vẫn dùng `/timeseries` range 7d/30d/year — tách biệt filter doanh thu, hợp plan.
- S11.3 có thể tái dùng params `/revenue` cho export.

## Kết luận

Story S11.2 **PASS**: lọc doanh thu end-to-end (AC-12-02) với API `/revenue`, nguồn `payment_transaction`, UI filter trên dashboard và chart đồng bộ. Không blocker — nợ nhỏ: smoke DB và apiClient 401 refresh (tùy chọn).
