# Review — S9.3: Crawler Langeek tuần + map A1-C2 + auto-publish

- **Ngày:** 2026-06-07
- **Verdict:** PASS

## Đối chiếu Acceptance Criteria

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-13-04 — crawl thành công → map A1–C2 → auto-publish | ✅ đạt | `run_sync` → `publish_batch` upsert `source=langeek`, `is_active=true`; không bước duyệt Admin. Fixture path (`LANGEEK_USE_FIXTURE`) + manual `POST /api/admin/crawler/jobs/run` hoạt động theo thiết kế. |
| BR10 — crawl định kỳ tuần | ✅ đạt | `jobs/crawler_scheduler.py` + APScheduler Chủ nhật 03:00; bật qua `CRAWLER_SCHEDULER_ENABLED=true`. Mặc định tắt — đúng plan (dev-safe). |
| FR-18 — crawler Langeek + orchestration BE | ✅ đạt | `speak-buddi-scrape` CLI/Playwright + `crawler_job.py` subprocess/fixture; ghi `content_crawl_job/log`. |
| FR-19 — mapping level/topic/word | ✅ đạt | `langeek_mapping.py` parse batch; `content_repo.upsert_langeek_*` match slug + `(topic_id, word)`. Unit smoke: fixture → 2 topics, 3 words. |
| §5.3 / S3.4 — chỉ soft-disable, không hard-delete | ✅ đạt | `soft_disable_langeek_words_not_in_batch` gọi `soft_delete_word` (UPDATE); không `DELETE FROM topic_word` trong luồng crawl. |
| AC-05-04 (liên epic) — progress giữ khi sync | ✅ đạt (logic) | Upsert/update giữ `topic_word.id`; soft-disable thay vì xóa row — khớp hợp đồng S3.4. Chưa chạy `verify_s3_4.sql` trên DB thật trong session review. |
| BR07 — chỉ admin | ✅ đạt | Router `dependencies=[Depends(require_admin)]`; FE route trong `AdminRoute`. |

## Kiểm chứng đã chạy

- **BE `python -m py_compile`** → pass: `admin_crawler`, `crawl_repo`, `crawler_job`, `crawler_publish`, `langeek_mapping`, `crawler_scheduler`, `main`.
- **BE mapping smoke** → `parse_scrape_payload(fixture)` → 2 topics, 3 words; `slugify` OK.
- **FE `npm run lint`** → 0 lỗi mới trong `features/admin/crawler/**`; 7 lỗi pre-existing RoadmapSection/RoadmapNode.
- **Code review** — API đủ theo plan (`sync-status`, `config`, `jobs`, `jobs/run`); Admin UI `/admin/crawler` bento layout, bỏ Commit/Discard (đúng SRS auto-publish).

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa
- **Live Langeek DOM selectors** trong `speak-buddi-scrape/src/langeek/crawl.js` chưa verify trên site thật — production crawl có thể trả batch rỗng/thiếu field. Fixture đủ cho dev/MVP; cần inspect DOM trước khi bật `LANGEEK_USE_FIXTURE=false`.
- **Smoke test PostgreSQL E2E** chưa chạy trong session review (cần `psql -f db/schema_crawler.sql` + trigger manual crawl + query `topic_word WHERE source='langeek'`).
- **Job `running` kẹt** nếu process crash giữa chừng — edge case hiếm; S9.4 có thể thêm timeout/cleanup.
- **FE chưa có màn cấu hình crawler** (PUT config schedule/rate_limit) — BE đã có; Admin chỉ trigger + xem log (chấp nhận cho S9.3 scope).

### 🟢 Quan sát thêm (ngoài phạm vi)
- Retry/notify/cache khi Langeek lỗi → **S9.4**.
- Admin edit/disable nội dung crawl → **S9.5**.
- PO xác nhận ToS/robots.txt trước production crawl (SRS TBD).

## Kết luận

Story S9.3 **PASS**: happy-path crawler Langeek (fixture + manual trigger + scheduler tuần) map và auto-publish topic/word đúng AC-13-04/BR10/FR-18/19, tuân hợp đồng soft-delete S3.4, Admin UI + API admin-only đầy đủ theo plan. Không blocker. Nợ nhỏ: smoke DB runtime + verify live DOM trước production.
