# Review — S9.2: Soft delete (is_active=false)

- **Ngày:** 2026-06-07
- **Verdict:** PASS

## Đối chiếu Acceptance Criteria

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-13-03 — disable + confirm → is_active=false | ✅ đạt | FE: `ConfirmDisableModal` trên Topics/Vocabulary/Tests/Editor; BE: DELETE `/api/admin/topics|words|tests/{id}` → UPDATE `is_active=FALSE` + commit + log `ADMIN_DISABLE_*`. |
| §5.3 — soft delete, không hard delete | ✅ đạt | Chỉ `UPDATE is_active=FALSE`; không `DELETE` row; không đổi schema. |
| Học viên không thấy inactive | ✅ đạt | Read-side giữ nguyên filter `is_active=TRUE` (`learning.py`, `quiz.py`, `roadmap_repo.py`) — không regress trong diff S9.2. |
| Admin thấy inactive + filter Status | ✅ đạt | Filter All/Active/Draft trên 3 list pages; badge Active/Draft; `include_inactive` wire đúng API. |

## Kiểm chứng đã chạy

- **FE `npm run lint`** → 7 errors pre-existing (`RoadmapSection.jsx`, `RoadmapNode.jsx`); **0 lỗi** trong `features/admin/**` (S9.2 scope).
- **BE `python -m py_compile`** → pass: `content_repo.py`, `quiz_repo.py`, `admin_content.py`.
- **Router** — 3 DELETE endpoint dưới `require_admin`, trả 404 khi id không tồn tại, 204 khi thành công.
- **Repo** — `soft_delete_topic/word/test` dùng UPDATE + `rowcount` check; idempotent khi gọi lại trên row đã inactive (PostgreSQL vẫn match row).

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa
- **Pagination vocabulary + filter Draft:** `AdminVocabularyPage` lọc inactive **client-side trên từng page** (`displayedItems`) trong khi `total`/pagination lấy từ API (mixed active+inactive). Khi nhiều từ, view Draft có thể thiếu/sai số trang. Chấp nhận cho MVP; cải thiện sau bằng query param `active_only`/`inactive_only` BE nếu cần.
- **`apiDelete` không refresh token 401** như `apiClient` — disable thất bại im lặng nếu token hết hạn giữa session; edge case hiếm, không chặn AC.

### 🟢 Quan sát thêm (ngoài phạm vi)
- Re-enable → S9.5 (đúng plan).
- Disable topic không cascade words/tests — đúng plan §6.
- Smoke test runtime PostgreSQL chưa chạy trong session review.

## Kết luận

Story S9.2 **PASS**: soft delete end-to-end cho topic/word/test với confirm modal (AC-13-03), đúng §5.3, UI admin filter/badge đầy đủ. Không blocker. Nợ nhỏ pagination filter Draft và smoke DB thủ công.
