# Review — S2.3: Cập nhật level trong profile → roadmap tương lai dùng level mới

- **Ngày:** 2026-06-06
- **Verdict:** PASS

> Re-review xác nhận 2 issue 🟡 minor từ review trước đã được fix. Nội dung AC gốc giữ nguyên (chi tiết ở review trước; tóm tắt lại dưới).

## Đối chiếu Acceptance Criteria

| AC | Kết quả | Ghi chú |
|---|---|---|
| AC-03-04 (persist target_level mới) | ✅ đạt | `user_repo.update_level()` UPDATE `target_level` + RETURNING; `routers/profile.py` gọi đúng. Roadmap (S2.2) computed on-the-fly từ cùng cột → phản ánh level mới. FE gọi `updateLevel` → `updateUser({ level })`. |
| AC-03-04 (FE user state cập nhật ngay) | ✅ đạt | `AuthContext.updateUser()` merge partial + dispatch → consumer re-render, không cần reload. |
| BR09 (BE chỉ nhận A1–C2, 400 nếu sai) | ✅ đạt | `routers/profile.py:44-50` `level.strip().upper()` + check `VALID_LEVELS` → HTTPException 400. Không bị fix mới làm hỏng. |
| BR09 (FE submit CEFR code) | ✅ đạt | `LevelSelector` chỉ render 6 code; không có input tự do. |
| Chỉ UPDATE target_level, giữ interests/daily_minutes/words_per_session | ✅ đạt | `update_level()` SQL chỉ SET `target_level` + `updated_at`. |
| Nút "Lưu thay đổi" disabled khi chọn cùng level | ✅ đạt | `ProfilePage.jsx:166,254` `hasChanged` + `disabled={!hasChanged \|\| submitting}`. Còn nguyên sau fix. |
| Confirm trước submit | ✅ đạt | `ProfilePage.jsx:308-340` Modal cảnh báo trước khi gọi API. Còn nguyên. |
| Toast sau thành công/lỗi | ✅ đạt | `ProfilePage.jsx:186,188` toast success/error. Còn nguyên. |
| Không log dữ liệu nhạy cảm | ✅ đạt | `routers/profile.py:61` chỉ log `user_id` + `level` (SRS §4.5). |
| Route /profile chặn khi chưa login | ✅ đạt | `<ProtectedRoute>` (FE) + `Depends(current_user)` (BE). |
| `npm run lint` không lỗi mới | ✅ đạt | Xem mục dưới. |

## Xác nhận 2 fix từ review trước

### Fix 1 — `onboarding_completed` không còn hardcode (🟡 → đã sửa)
- `routers/profile.py:63-66`:
  ```python
  return UpdateLevelOut(
      level=result["target_level"],
      onboarding_completed=result["target_level"] is not None,
  )
  ```
  Không còn `=True` cứng. Giá trị derive từ `target_level`.
- **Nhất quán với codebase:** khớp đúng pattern ở `schemas/auth.py:44` `"onboarding_completed": user.get("level") is not None`. Cùng triết lý "đã có level ⇒ đã onboard". Đúng và đồng bộ.

### Fix 2 — className responsive đã gán (🟡 → đã sửa)
- `ProfilePage.jsx:213` `<div style={styles.twoCol} className="profile-settings-two-col">` ✅
- `ProfilePage.jsx:215` `<nav style={styles.settingsNav} className="profile-settings-nav" ...>` ✅
- `MODAL_CSS` (dòng 511-527) vẫn định nghĩa cả 2 class trong `@media (max-width: 768px)` (`flex-direction: column` cho two-col, `position: static / flex-direction: row` cho nav) ✅
- `<style>{MODAL_CSS}</style>` được inject ở dòng 314 (trong Modal, luôn mount trong cây render của trang) → CSS có hiệu lực toàn trang, media query nay khớp className thật. ✅

## Kiểm chứng đã chạy

- `cd speak-buddi && npm run lint` → **5 errors, tất cả trong `RoadmapSection.jsx` (S2.2, pre-existing):** `no-unused-vars` (CR, isCurrent, totalXP, progress) + `react-hooks/rules-of-hooks`. **Không có lỗi mới ở `ProfilePage.jsx` hay file S2.3 nào khác.** Hai fix FE/BE không phát sinh lỗi lint.
- Đọc trực tiếp `routers/profile.py`, `schemas/profile.py`, `schemas/auth.py`, `ProfilePage.jsx` — đối chiếu nội dung context fix → khớp.

## Phát hiện

### 🔴 Phải sửa (blocker)
- Không có.

### 🟡 Nên sửa
- Không còn (2 issue minor của review trước đã xử lý đúng).

### 🟢 Quan sát thêm (ngoài phạm vi)
- 5 lỗi lint trong `RoadmapSection.jsx` thuộc S2.2 — xử lý ở review/patch của S2.2, không chặn S2.3.
- `levels.js` và `LevelStep.jsx` vẫn giữ 2 bản LEVELS/BADGE_COLORS trùng nội dung — nợ kỹ thuật đã ghi nhận, refactor lên `shared/constants` ở story sau.

## Kết luận

Cả 2 issue minor từ review trước đã được fix đúng: `onboarding_completed` nay derive từ `target_level is not None` (nhất quán với `schemas/auth.py`) thay vì hardcode `True`; và `className="profile-settings-two-col"` / `profile-settings-nav` đã được gán vào JSX, khớp với media query trong `MODAL_CSS` nên responsive collapse mobile đã có hiệu lực. Không có regression: BR09 validation, nút Save disabled, confirm Modal, toast đều còn nguyên. Lint sạch (không lỗi mới; 5 lỗi còn lại là pre-existing của S2.2). → **PASS**.
