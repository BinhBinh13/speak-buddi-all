services:
  backend:
    image: ghcr.io/${GITHUB_REPO}/speak-buddi-be:latest
    container_name: speak-buddi-be
    restart: unless-stopped
    env_file:
      - .env.backend
    ports:
      - "127.0.0.1:8000:8000"
