# Plan: S4.3 — Làm bài + Chặn nộp khi thiếu đáp án

**Epic:** EPIC-4 — Bài kiểm tra từ vựng  
**Story:** S4.3 — Làm bài + chặn nộp khi thiếu đáp án + highlight câu thiếu  
**AC nguồn:** AC-06-02, §5.2  
**Priority:** P1  
**Ngày lập:** 2026-06-07  
**Màn UI tham chiếu:** Không có màn UI riêng cho S4.3 — dùng design system từ `bai_kiem_tra_desktop`

---

## 1. Mục tiêu

Thêm luồng **submit bài kiểm tra** hoàn chỉnh:
1. Bắt đầu làm bài → tạo attempt trên backend (`POST /api/tests/{test_id}/attempts`).
2. Sau khi trả lời hết câu → hiển thị **Review Screen** (danh sách tất cả câu + trạng thái).
3. Bấm **"Nộp bài"** → client-side validation:
   - Nếu còn câu chưa trả lời (hoặc bị skip) → highlight đỏ + toast "Vui lòng trả lời tất cả câu hỏi trước khi nộp." → **chặn submit**.
   - Nếu tất cả câu đã có đáp án → gọi `POST /api/attempts/{attempt_id}/submit` → lưu DB → navigate result placeholder.
4. Từ Review Screen, user có thể click vào câu chưa trả lời để quay lại câu đó.

**Không trong scope S4.3:** trang kết quả chi tiết, màn hình điểm số (S4.4).

---

## 2. Phân tích code hiện tại

| File | Trạng thái | Ghi chú |
|---|---|---|
| `QuizPage.jsx` | Đã có | Sequential flow: trả lời 1-1 → `finished=true` → placeholder screen |
| `QuizRunner.jsx` | Đã có | Dispatcher 4 loại, `handleSkip()` cho unsupported types |
| `FillBlankQuestion.jsx` | Đã có | Disable "Kiểm tra" nếu input rỗng — đã validate |
| `GrammarMappingQuestion.jsx` | Đã có | Disable "Kiểm tra" nếu chưa nối đủ cặp — đã validate |
| `quizService.js` | Cần mở rộng | Hiện có `getTest`, `getQuestions` — chưa có start/submit attempt |
| `routers/quiz.py` | Cần mở rộng | Hiện chỉ có GET endpoints — chưa có attempt endpoints |
| `schemas/quiz.py` | Đã có | `QuizAttemptCreate`, `QuizAttemptOut`, `QuizAttemptAnswerSubmit` sẵn sàng |
| `repositories/quiz_repo.py` | Đã có | `create_attempt`, `upsert_attempt_answer`, `submit_attempt` sẵn sàng |
| `services/quiz_service.py` | Đã có | `calculate_score`, `check_fill_blank_answer` sẵn sàng |
| `App.jsx` | Cần mở rộng | Route `/quiz/:testId/result/:attemptId` chưa có |

---

## 3. Các file cần sửa / tạo

```
speak-buddi-be/
  schemas/quiz.py          ← thêm QuizSubmitRequest
  routers/quiz.py          ← thêm 2 endpoint: start attempt + submit
speak-buddi/
  src/features/quiz/
    services/quizService.js  ← thêm startAttempt(), submitAttempt()
    QuizPage.jsx             ← thêm state, Review Screen, handleSubmit, handleGoToQuestion
    components/QuizReview.jsx  ← component mới (Review Screen)
  src/app/App.jsx            ← thêm route /quiz/:testId/result/:attemptId (placeholder)
```

---

## 4. Chi tiết triển khai

### BE-1: schemas/quiz.py — thêm QuizSubmitRequest

```python
class QuizSubmitRequest(BaseModel):
    """Request body nộp bài (S4.3): danh sách câu trả lời của user."""
    answers: list[QuizAttemptAnswerSubmit]
```

Thêm vào cuối file, sau `QuizAttemptAnswerOut`.

---

### BE-2: routers/quiz.py — POST /api/tests/{test_id}/attempts

```python
@router.post("/tests/{test_id}/attempts", response_model=QuizAttemptOut, status_code=201)
async def start_attempt(
    test_id: uuid_mod.UUID,
    user: dict = Depends(current_user),
    db: AsyncSession = Depends(get_db),
) -> QuizAttemptOut:
    """
    Tạo lượt làm bài mới cho user.
    404 nếu test không tồn tại hoặc is_active=FALSE.
    """
    test_row = await quiz_repo.get_test_by_id(db, str(test_id))
    if test_row is None or not test_row.get("is_active", False):
        raise HTTPException(status_code=404, detail="Test not found or inactive")

    questions = await quiz_repo.get_questions_by_test(db, str(test_id))
    total_q = len(questions)

    attempt = await quiz_repo.create_attempt(
        db, user_id=user["sub"], test_id=str(test_id), total_q=total_q
    )
    await db.commit()
    log.info("START_ATTEMPT  user=%s  test=%s  attempt=%s", user["sub"], test_id, attempt["id"])
    return QuizAttemptOut(**attempt)
```

---

### BE-3: routers/quiz.py — POST /api/attempts/{attempt_id}/submit

Logic:
1. Lấy attempt, kiểm tra tồn tại + thuộc về user + status='in_progress'.
2. Load tất cả câu hỏi + đáp án của test đó.
3. Validate: `len(body.answers) == attempt.total_questions` (422 nếu không khớp).
4. Với mỗi câu trả lời:
   - `flashcard`: `is_correct = True` (user tự đánh giá, không có đáp án đúng/sai)
   - `multiple_choice`: lookup `quiz_answer_id` trong danh sách answers → lấy `is_correct`
   - `fill_blank`: lấy đáp án đúng (answer có `is_correct=True`) → `check_fill_blank_answer(text_answer, correct_text)`
   - `grammar_mapping`: `is_correct = True` (S4.3 không chấm chi tiết pairs, defer S4.4)
5. Gọi `upsert_attempt_answer()` cho mỗi câu.
6. Gọi `calculate_score(correct, total)`.
7. Gọi `submit_attempt(attempt_id, correct, wrong, score)`.
8. Commit, return `QuizAttemptOut`.

```python
@router.post("/attempts/{attempt_id}/submit", response_model=QuizAttemptOut)
async def submit_quiz(
    attempt_id: uuid_mod.UUID,
    body: QuizSubmitRequest,
    user: dict = Depends(current_user),
    db: AsyncSession = Depends(get_db),
) -> QuizAttemptOut:
    """
    Nộp bài kiểm tra.
    422 nếu số câu trả lời không khớp total_questions.
    403 nếu attempt không thuộc về user hiện tại.
    409 nếu attempt đã submitted.
    """
    attempt = await quiz_repo.get_attempt_by_id(db, str(attempt_id))
    if attempt is None:
        raise HTTPException(status_code=404, detail="Attempt not found")
    if attempt["user_id"] != user["sub"]:
        raise HTTPException(status_code=403, detail="Forbidden")
    if attempt["status"] != "in_progress":
        raise HTTPException(status_code=409, detail="Attempt already submitted")

    total_q = attempt["total_questions"]
    if len(body.answers) != total_q:
        raise HTTPException(
            status_code=422,
            detail=f"Expected {total_q} answers, got {len(body.answers)}"
        )

    # Load questions + answers để chấm
    questions = await quiz_repo.get_questions_by_test(db, attempt["vocabulary_test_id"])
    q_map = {q["id"]: q for q in questions}  # question_id → question dict

    correct = 0
    wrong = 0

    for ans in body.answers:
        q_id = str(ans.quiz_question_id)
        q = q_map.get(q_id)
        if q is None:
            continue

        q_type = q["question_type"]
        is_correct = False

        if q_type == "flashcard":
            is_correct = True  # self-rating

        elif q_type == "multiple_choice":
            if ans.quiz_answer_id:
                answer_id = str(ans.quiz_answer_id)
                matched = next(
                    (a for a in q.get("answers", []) if a["id"] == answer_id),
                    None
                )
                is_correct = matched["is_correct"] if matched else False

        elif q_type == "fill_blank":
            correct_answer = next(
                (a for a in q.get("answers", []) if a.get("is_correct")),
                None
            )
            if correct_answer and ans.text_answer:
                from services.quiz_service import check_fill_blank_answer
                is_correct = check_fill_blank_answer(
                    ans.text_answer, correct_answer["answer_text"]
                )

        elif q_type == "grammar_mapping":
            is_correct = True  # S4.3: self-grading; S4.4 có thể thêm proper check

        if is_correct:
            correct += 1
        else:
            wrong += 1

        await quiz_repo.upsert_attempt_answer(
            db,
            attempt_id=str(attempt_id),
            question_id=q_id,
            answer_id=str(ans.quiz_answer_id) if ans.quiz_answer_id else None,
            text_answer=ans.text_answer,
            is_correct=is_correct,
        )

    from services.quiz_service import calculate_score
    score = calculate_score(correct, total_q)
    result = await quiz_repo.submit_attempt(db, str(attempt_id), correct, wrong, score)
    await db.commit()
    log.info("SUBMIT_ATTEMPT  attempt=%s  score=%.1f%%", attempt_id, score)
    return QuizAttemptOut(**result)
```

---

### FE-1: quizService.js — thêm startAttempt, submitAttempt

```js
export async function startAttempt(testId) {
  return apiClient(`/api/tests/${testId}/attempts`, { method: "POST" });
}

export async function submitAttempt(attemptId, body) {
  return apiClient(`/api/attempts/${attemptId}/submit`, {
    method: "POST",
    body: JSON.stringify(body),
  });
}
```

---

### FE-2: QuizPage.jsx — state mới + logic

**State thêm vào:**
```js
const [attemptId, setAttemptId] = useState(null);
const [submitting, setSubmitting] = useState(false);
const [submitError, setSubmitError] = useState(null);
const [unansweredIds, setUnansweredIds] = useState(new Set());
```

**Sửa fetchData():**
```js
// Sau khi fetch test + questions thành công:
const [testData, questionsData, attemptData] = await Promise.all([
  getTest(testId),
  getQuestions(testId),
  startAttempt(testId),
]);
setAttemptId(attemptData.id);
```

**Thêm handleSubmit:**
```js
async function handleSubmit() {
  // Client-side validation — §5.2
  const unanswered = questions.filter(
    (q) => !userAnswers[q.id] || userAnswers[q.id].skipped
  );
  if (unanswered.length > 0) {
    setUnansweredIds(new Set(unanswered.map((q) => q.id)));
    setSubmitError("Vui lòng trả lời tất cả câu hỏi trước khi nộp.");
    return;
  }

  const answers = questions.map((q) => ({
    quiz_question_id: q.id,
    quiz_answer_id: userAnswers[q.id]?.selectedAnswerId ?? null,
    text_answer: userAnswers[q.id]?.textAnswer ?? null,
  }));

  setSubmitting(true);
  setSubmitError(null);
  try {
    const result = await submitAttempt(attemptId, { answers });
    navigate(`/quiz/${testId}/result/${result.id}`);
  } catch (err) {
    setSubmitError(err.message || "Có lỗi xảy ra, vui lòng thử lại.");
  } finally {
    setSubmitting(false);
  }
}
```

**Thêm handleGoToQuestion:**
```js
function handleGoToQuestion(index) {
  setCurrentIndex(index);
  setFinished(false);
  setUnansweredIds(new Set());
  setSubmitError(null);
}
```

**Thay finished placeholder bằng `<QuizReview />`:**
```jsx
if (finished) {
  return (
    <QuizReview
      test={test}
      questions={questions}
      userAnswers={userAnswers}
      unansweredIds={unansweredIds}
      submitError={submitError}
      submitting={submitting}
      attemptId={attemptId}
      onSubmit={handleSubmit}
      onRetry={handleRetry}
      onGoToQuestion={handleGoToQuestion}
      onBack={handleBack}
    />
  );
}
```

---

### FE-3: components/QuizReview.jsx — component mới

Props: `{ test, questions, userAnswers, unansweredIds, submitError, submitting, attemptId, onSubmit, onRetry, onGoToQuestion, onBack }`

**Layout:**
```
[Header: test.title + progress bar 100%]
[Review Section Title: "Kiểm tra lại đáp án"]
[Danh sách câu hỏi]:
  Mỗi câu: số thứ tự + type label + trạng thái
    - Đã trả lời: ✓ màu #006c49 (xanh lá)
    - Chưa trả lời / skip: ✗ màu #ba1a1a (đỏ) + "Chưa trả lời" badge
    - Câu trong unansweredIds: border: 2px solid #ba1a1a + nhấp để quay lại
[Toast error: submitError]
[Footer buttons]:
  - "Nộp bài" (primary, #3525cd) — disabled nếu submitting hoặc !attemptId
  - "Làm lại" (secondary)
```

**Highlight unanswered:**
```jsx
const isUnanswered = unansweredIds.has(q.id) || !userAnswers[q.id] || userAnswers[q.id]?.skipped;
// border: `2px solid ${isUnanswered ? "#ba1a1a" : "#e4e1ee"}`
// Nếu isUnanswered → thêm badge "✗ Chưa trả lời" + onClick={() => onGoToQuestion(index)}
```

---

### FE-4: App.jsx — thêm route result page

```jsx
<Route path="/quiz/:testId/result/:attemptId" element={<QuizResultPage />} />
```

Tạo file `speak-buddi/src/features/quiz/QuizResultPage.jsx` — placeholder (S4.4 sẽ implement chi tiết):
```jsx
// Placeholder — S4.4 sẽ thêm màn kết quả chi tiết
export default function QuizResultPage() {
  const { testId, attemptId } = useParams();
  const navigate = useNavigate();
  return (
    <div style={{ minHeight: "100vh", display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", background: "#fcf8ff" }}>
      <div style={{ fontSize: 64 }}>🎉</div>
      <h1 style={{ color: "#1b1b24" }}>Nộp bài thành công!</h1>
      <p style={{ color: "#777587" }}>Kết quả chi tiết sẽ có trong phiên bản tiếp theo.</p>
      <button onClick={() => navigate(-1)} style={{ padding: "14px 32px", borderRadius: 12, background: "#3525cd", color: "#fff", border: "none", cursor: "pointer", fontWeight: 600, fontSize: 15 }}>
        Quay lại
      </button>
    </div>
  );
}
```

---

## 5. AC Mapping

| AC | Triển khai |
|---|---|
| AC-06-02: Chặn nộp khi thiếu đáp án | `handleSubmit()` validate `unansweredIds` trước khi gọi API |
| AC-06-02: Highlight câu thiếu | `QuizReview.jsx` render border đỏ cho câu trong `unansweredIds` |
| AC-06-02: Thông báo "Vui lòng trả lời tất cả câu hỏi" | `submitError` state + toast trong `QuizReview.jsx` |
| §5.2: Client-side validation trước API | `handleSubmit()` check local state trước khi `submitAttempt()` |

---

## 6. Rủi ro & Edge Cases

| Rủi ro | Xử lý |
|---|---|
| `startAttempt` lỗi (network/server) | Hiển thị error state, disable "Nộp bài" nếu `!attemptId` |
| Question unsupported type bị `handleSkip()` | `skipped: true` trong `userAnswers` → bị detect là unanswered, highlight đỏ |
| `grammar_mapping is_correct = True` | Acceptable cho S4.3 (self-grading); ghi chú trong code |
| Duplicate submit (user bấm 2 lần) | `disabled={submitting}` trên nút "Nộp bài" |
| `userAnswers[q.id]` format khác nhau theo type | `selectedAnswerId` (MC), `textAnswer` (fill_blank), `selfRating` (flashcard), `mappingPairs` (grammar) — chỉ `quiz_answer_id` và `text_answer` cần cho API |
| Không có route result page | Tạo placeholder `QuizResultPage.jsx` + route trong App.jsx |
