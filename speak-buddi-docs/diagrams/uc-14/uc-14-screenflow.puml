@startuml
title UC14 — Admin manages payment plans — Screen Flow
skinparam activityBackgroundColor #FEFEFE
skinparam activityBorderColor #333333
start
:Open AdminPaymentPlanPage;
:User attempts to create/update/disable payment plans;
if (Access and input valid?) then (yes)
  :Call PaymentPlanAdminService;
  :Persist/read PaymentPlan data if needed;
  :Display success state;
else (no)
  :Show validation/access error;
  :Keep user input where possible;
endif
stop
@enduml
