# Implement Log — S8.4: Paid User chọn/đổi voice/model ElevenLabs; chặn free user

- **Ngày:** 2026-06-07
- **Plan tham chiếu:** plan/S8.4-plan.md
- **Kết quả:** Hoàn thành

## Màn UI tham chiếu
- `speak-buddi-docs/ui/cai_dat_giong_doc_desktop/code.html` + `screen.png`
- `speak-buddi-docs/ui/speak_buddi/DESIGN.md`

## Thay đổi theo file
| File | Loại | Mô tả |
|---|---|---|
| speak-buddi-be/db/schema_voice.sql | thêm | Bảng `elevenlabs_voice_model` + `user_voice_preference` + trigger |
| speak-buddi-be/db/seed_dev.sql | sửa | Seed 3 voice (Sarah/James/Chloe), default khớp `ELEVENLABS_VOICE_ID` |
| speak-buddi-be/schemas/voice.py | thêm | Pydantic `VoiceModelOut`, `VoicePreferenceOut`, `SetVoicePreferenceRequest` |
| speak-buddi-be/repositories/voice_repo.py | thêm | list/get/upsert preference + `get_voice_id_for_user` |
| speak-buddi-be/routers/voice.py | thêm | `GET/PUT /api/voice/*` với `require_paid`, audit log |
| speak-buddi-be/auth/deps.py | sửa | Thêm `optional_current_user` cho TTS backward-compat |
| speak-buddi-be/services/tts_service.py | sửa | Tham số `voice_id`/`model_id` tùy chọn |
| speak-buddi-be/routers/ai.py | sửa | `/speak` + `/tts` áp voice preference khi có JWT |
| speak-buddi-be/main.py | sửa | `include_router(voice.router)` |
| speak-buddi-be/README.md | sửa | Thêm schema_voice + 3 endpoint voice |
| speak-buddi/src/features/settings/services/voiceService.js | thêm | API client 3 hàm |
| speak-buddi/src/features/settings/VoiceSettingsPage.jsx | thêm | Màn cài đặt giọng đọc theo mockup (Việt hóa) |
| speak-buddi/src/app/App.jsx | sửa | Route `/settings/voice` trong `PaidRoute` |
| speak-buddi/src/shared/components/AppLayout.jsx | sửa | Page title cho `/settings/voice` |

## Đối chiếu Acceptance Criteria
- [x] AC-11-01 — `GET /api/voice/models` + FE render lưới voice active
- [x] AC-11-02 — `PUT /api/voice/preference` UPSERT DB; FE highlight + lưu; `/tts`/`/speak` dùng preference
- [x] AC-11-03 / BR06 — `require_paid` → 403; `PaidRoute` redirect free user → `/pricing`; banner nâng cấp khi API 403
- [x] §4.5 — Audit `VOICE_PREF_SET user=... voice_model=...`; không log secret/audio
- [x] §4.8 — Grid responsive, nút Lưu min-height 44px

## Lệnh đã chạy & kết quả
- `python -c "import ast..."` (7 file BE) → pass
- `npm run lint` → 7 lỗi **có sẵn** ở `RoadmapSection.jsx`/`RoadmapNode.jsx`; **không có lỗi** ở file S8.4 mới

## Cách test thủ công
1. Chạy `psql ... -f db/schema_voice.sql` rồi `seed_dev.sql` (hoặc migrate full).
2. Login Paid User → `/settings/voice` → thấy 3 giọng, chọn + Lưu → reload giữ lựa chọn.
3. Gọi `/tts` với JWT Paid User đã chọn voice → ElevenLabs dùng `voice_id` mới.
4. Login free Student → `/settings/voice` redirect `/pricing`; API voice trả 403.

## Ngoài phạm vi / nợ kỹ thuật / TODO
- Speaking Rate + Auto-Play trong mockup: **ẩn** (không thuộc AC-11).
- Preview audio từng voice: chưa làm (tùy chọn plan).
- Admin CRUD bảng voice: seed cứng; story Admin sau.
- Chưa smoke test E2E trên DB thật trong session này (cần Reviewer verify).
