# Implement Log — S1.8: Phân quyền role + subscription state

**Story:** S1.8 · **Epic:** EPIC-1  
**Ngày:** 2026-06-06  
**Verdict:** Hoàn thành — chuyển sang `review`

---

## Tóm tắt thay đổi

### Backend — `speak-buddi-be/main.py`

| Task | Thay đổi |
|---|---|
| BE-1 | Thêm `is_paid: False` vào `demo@speakbuddi.com`; thêm 2 mock users mới: `admin@speakbuddi.com` (role=admin) và `paid@speakbuddi.com` (role=student, is_paid=True) |
| BE-2 | Thêm helper `_get_is_paid(user)` — đọc `user.get("is_paid", False)`; TODO S8.2 để migrate sang DB query |
| BE-3 | Cập nhật `_make_access(user)` — thêm claims `role` và `is_paid` vào JWT payload |
| BE-4 | Thêm `require_admin` Depends (403 nếu role != 'admin') và `require_paid` Depends (403 nếu is_paid == False) |
| BE-5 | Thêm helper `_user_response(user)` — chuẩn hóa response shape dùng chung cho tất cả auth endpoints |
| BE-6 | Cập nhật 4 endpoints: `POST /api/auth/login`, `POST /api/auth/register`, `POST /api/auth/google` dùng `_user_response()`; `GET /api/auth/me` tra MOCK_USERS từ `sub` claim và trả `_user_response` thay vì raw JWT payload |

### Frontend — `speak-buddi/src/`

| Task | File | Thay đổi |
|---|---|---|
| FE-1 | `shared/services/authService.js` | Thêm `KEY_USER`, `getUser()`, `setUser()`, `clearUser()`; cập nhật `clearTokens()` gọi `clearUser()` |
| FE-2 | `shared/auth/AuthContext.jsx` | Thêm `user` state, `isAdmin`, `isPaid`; cập nhật `login()` nhận `{ access_token, refresh_token, user }` và lưu user vào localStorage; `logout()` xóa user |
| FE-3 | `shared/auth/AdminRoute.jsx` | Tạo mới — guard redirect `/` nếu không phải admin |
| FE-4 | `shared/auth/PaidRoute.jsx` | Tạo mới — guard redirect `/pricing` nếu is_paid=false |
| FE-5 | `app/App.jsx` | Import `AdminRoute`, `PaidRoute`; thêm placeholder `<Route element={<AdminRoute />}>` và `<Route element={<PaidRoute />}>` |
| FE-6 | `features/auth/LoginPage.jsx` | Cập nhật 2 callers của `login()` truyền thêm `user: data.user` |
| FE-6 | `features/auth/RegisterPage.jsx` | Cập nhật caller `login()` truyền thêm `user: data.user` |
| FE-6 | `features/auth/OAuthCallbackPage.jsx` | Cập nhật `login(token)` → `login({ access_token: token, refresh_token: null, user: null })` |

---

## Files thay đổi

```
speak-buddi-be/main.py                          (sửa)
speak-buddi/src/shared/services/authService.js  (sửa)
speak-buddi/src/shared/auth/AuthContext.jsx     (sửa)
speak-buddi/src/shared/auth/AdminRoute.jsx      (tạo mới)
speak-buddi/src/shared/auth/PaidRoute.jsx       (tạo mới)
speak-buddi/src/app/App.jsx                     (sửa)
speak-buddi/src/features/auth/LoginPage.jsx     (sửa)
speak-buddi/src/features/auth/RegisterPage.jsx  (sửa)
speak-buddi/src/features/auth/OAuthCallbackPage.jsx (sửa)
```

---

## Kiểm tra

- **Python syntax:** `python -c "import ast; ast.parse(open('main.py', encoding='utf-8').read())"` → OK
- **ESLint (S1.8 files):** `eslint AdminRoute.jsx PaidRoute.jsx AuthContext.jsx authService.js App.jsx` → 0 errors
- **ESLint (toàn dự án):** 5 lỗi pre-existing trong `RoadmapSection.jsx` (không liên quan S1.8)

---

## Acceptance Criteria đạt

| AC | Kết quả |
|---|---|
| AC-S1.8-01 | ✅ JWT claims có `role` và `is_paid` |
| AC-S1.8-02 | ✅ `require_admin` → 403 nếu role != 'admin' |
| AC-S1.8-03 | ✅ `require_paid` → 403 nếu is_paid == false |
| AC-S1.8-04 | ✅ `GET /api/auth/me` trả `role`, `is_paid` |
| AC-S1.8-05 | ✅ login/register/google response có `user.role`, `user.is_paid` |
| AC-S1.8-06 | ✅ MOCK_USERS có admin + paid demo |
| AC-S1.8-07 | ✅ `_get_is_paid()` với TODO S8.2 |
| AC-S1.8-08 | ✅ `AuthContext` expose `user`, `isAdmin`, `isPaid` |
| AC-S1.8-09 | ✅ `AdminRoute` redirect `/` nếu không phải admin |
| AC-S1.8-10 | ✅ `PaidRoute` redirect `/pricing` nếu is_paid=false |
| AC-S1.8-11 | ✅ `user` lưu localStorage; xóa khi logout |
| AC-S1.8-12 | ✅ Không log role/subscription vào server/console |

---

## Ghi chú kỹ thuật

- `_user_response()` helper tập trung shape, giúp khi migrate sang DB chỉ cần sửa 1 chỗ.
- `GET /api/auth/me` tra ngược MOCK_USERS từ `sub` để lấy `is_paid` mới nhất (tránh stale từ JWT).
- `OAuthCallbackPage` dùng flow cũ (query param `?token=`) — giữ tương thích bằng `login({ access_token: token, ... })`.
- Admin route và Paid route hiện chỉ là placeholder; route thật sẽ thêm ở S9.x và S7.3/S8.4.
- UI screen tham chiếu: không có màn UI mới (S1.8 là logic/auth, không có giao diện riêng).
