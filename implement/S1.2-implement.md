# Implement Log — S1.2: Trang landing + pricing công khai

- **Ngày:** 2026-06-06
- **Plan tham chiếu:** plan/S1.2-plan.md
- **Kết quả:** Hoàn thành

## Màn UI đã tham chiếu
- `speak-buddi-docs/ui/landing_page_desktop/code.html` + `screen.png`
- `speak-buddi-docs/ui/pricing_page_desktop/code.html` + `screen.png`
- `speak-buddi-docs/ui/speak_buddi/DESIGN.md` (design system)

## Thay đổi theo file

| File | Loại | Mô tả |
|---|---|---|
| `speak-buddi/index.html` | sửa | Thêm `<link>` Be Vietnam Pro (400/500/600/700), đổi `lang="vi"`, đổi `<title>` thành "SpeakBuddi" |
| `speak-buddi/src/shared/constants/designTokens.js` | tạo | Token màu/font/typography/radius/spacing/shadow theo DESIGN.md — tách riêng, không sửa theme.js cũ |
| `speak-buddi/src/shared/components/PublicNavbar.jsx` | sửa | Việt hóa link (Tính năng / Bảng giá / Đăng nhập), CTA "Bắt đầu miễn phí" → `/login`; "Bảng giá" dùng `<Link to="/pricing">`, brand "SpeakBuddi" màu indigo; bỏ `user` prop (App không truyền), giữ AppNav/AppRight cho isApp |
| `speak-buddi/src/shared/components/PublicFooter.jsx` | sửa | Layout mới: brand SpeakBuddi (indigo) + copyright VI + 4 link (Điều khoản/Bảo mật/Liên hệ/Trợ giúp); bỏ multi-column footer cũ |
| `speak-buddi/src/features/landing/LandingPage.jsx` | sửa | Dùng `UI` token thay COLORS/FONTS cũ; chỉ render Hero → Stats → Features → PricingSection (bỏ HowItWorks/Testimonials/CTABanner theo mockup) |
| `speak-buddi/src/features/landing/components/HeroSection.jsx` | sửa | Tiêu đề VI "Luyện nói tiếng Anh cùng AI — mọi lúc, mọi nơi"; badge "Phát âm xuất sắc! / Độ chính xác 98%" với `MdMic`; 2 CTA → `/login` (TODO: /register) và `/pricing`; bỏ Waveform/demo card/typing animation |
| `speak-buddi/src/features/landing/components/StatsBar.jsx` | sửa | 3 số liệu VI: 100,000+ Người dùng / 500+ Chủ đề giao tiếp / 10,000+ Từ vựng; nền `surfaceContainerLow`, màu primary indigo; bỏ AnimatedCounter |
| `speak-buddi/src/features/landing/components/FeaturesSection.jsx` | sửa | 6 feature card VI với react-icons (MdChat/MdGraphicEq/MdMenuBook/MdMap/MdQuiz/MdTranslate); 3 tone màu (primary/secondary/tertiary); grid responsive 1→2→3 cột |
| `speak-buddi/src/features/landing/components/PricingSection.jsx` | sửa | Teaser "Mở khóa toàn bộ tiềm năng"; 2 card (Cơ bản Miễn phí + Pro 99k/tháng gold-border badge "Phổ biến nhất"); CTA Pro → `/pricing` |
| `speak-buddi/src/features/pricing/PricingPage.jsx` | tạo | Trang `/pricing`: PublicNavbar + header "Bảng giá" + 2 plan card (Free 0đ / Pro 149.000đ) + FAQ 3 mục + PublicFooter; gold-gradient wrapper Pro card |
| `speak-buddi/src/app/App.jsx` | sửa | Thêm `import PricingPage` và `<Route path="/pricing" element={<PricingPage />} />` (public, không bọc auth guard) |

## Đối chiếu Acceptance Criteria

- [x] **AC-01-01** — Route `/` trả về `LandingPage` không có guard auth; render Hero + Stats + Features + PricingTeaser đầy đủ, không gọi API backend.
- [x] **AC-01-02** — Route `/pricing` trả về `PricingPage` không có guard auth; hiển thị header "Bảng giá", 2 gói (Free + Pro với badge "Phổ biến"), FAQ 3 mục.
- [x] **Điều hướng CTA** — "Bắt đầu miễn phí" → `/login` (TODO /register S1.4); "Xem bảng giá" → `/pricing`; navbar "Bảng giá" → `/pricing`; "Đăng nhập" → `/login`.
- [x] **Khớp mockup** — layout/thứ tự khối/màu indigo/gold Pro/font Be Vietnam Pro/nội dung VI theo `landing_page_desktop` và `pricing_page_desktop`.
- [x] **NFR §4.8** — Responsive: feature grid 1→2→3 cột, pricing 1→2 cột, stats 1→3 cột; touch target ≥ 44px (minHeight: 44 trên tất cả button/link CTA).
- [x] **Không vỡ dashboard/speaking** — `theme.js` cũ (COLORS/FONTS emerald/navy) giữ nguyên, token mới tách ra `designTokens.js`.

## Lệnh đã chạy & kết quả

- `npm run lint` → **11 lỗi còn lại đều là pre-existing** (LoginPage.jsx, OAuthCallbackPage.jsx, RoadmapSection.jsx, SpeakingPage.jsx, speechService.js) — không có lỗi mới từ code S1.2. 3 lỗi ban đầu trong code mới (duplicate `display` key, `useNavigate` unused, `included` unused) đã được sửa.

## Cách test thủ công

1. `cd speak-buddi && npm install && npm run dev`
2. Mở trình duyệt ẩn danh (không có token) tại `http://localhost:5173/`
   - Kiểm tra: PublicNavbar hiển thị "SpeakBuddi" (indigo), "Tính năng", "Bảng giá", "Đăng nhập", nút "Bắt đầu miễn phí"
   - Kiểm tra: Hero section có tiêu đề VI + 2 nút CTA
   - Kiểm tra: Stats 3 số liệu VI trên nền surfaceContainerLow
   - Kiểm tra: 6 feature card với icon react-icons
   - Kiểm tra: Pricing teaser 2 card (Cơ bản / Pro với gold border + badge)
   - Kiểm tra: Footer VI (Điều khoản / Bảo mật / Liên hệ / Trợ giúp)
3. Click "Bảng giá" hoặc "Xem bảng giá" → phải điều hướng tới `/pricing`
4. Tại `/pricing`: header "Bảng giá", 2 plan card (Free 0đ / Pro 149.000đ với gold wrapper), FAQ 3 mục
5. Responsive: DevTools → 375px (mobile), 768px (tablet), 1024px, 1440px — grid co đúng, không tràn ngang
6. Trang `/dashboard` và `/speaking` không bị ảnh hưởng (theme.js cũ giữ nguyên)

## Ngoài phạm vi / nợ kỹ thuật / TODO

- **TODO `/register`**: CTA "Bắt đầu miễn phí" trỏ tạm `/login` vì route `/register` thuộc S1.4 chưa build. Đã ghi comment `// TODO` trong code.
- **Giá Pro lệch giữa 2 mockup**: landing teaser hiển thị `99k/tháng` (theo `landing_page_desktop`), pricing page hiển thị `149.000đ/tháng` (theo `pricing_page_desktop`). Giữ nguyên theo từng mockup, chờ xác nhận từ S10.1.
- **Ảnh hero**: dùng gradient placeholder vì mockup dùng ảnh từ Google CDN ngoài. Cần asset thật hoặc unsplash khi có.
- **HowItWorks / Testimonials / CTABanner**: đã bỏ khỏi LandingPage theo quyết định plan (không có trong mockup). Các file component vẫn giữ lại trong `landing/components/`, không xóa.
- **Lỗi lint pre-existing** (10 errors, 1 warning) ở các file không thuộc S1.2: RoadmapSection, SpeakingPage, LoginPage, OAuthCallbackPage, speechService — ngoài phạm vi story này.
