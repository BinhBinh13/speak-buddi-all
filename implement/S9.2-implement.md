# Implement Log — S9.2: Soft delete (is_active=false)

- **Ngày:** 2026-06-07
- **Plan tham chiếu:** plan/S9.2-plan.md
- **Kết quả:** Hoàn thành

## Thay đổi theo file

| File | Loại | Mô tả |
|---|---|---|
| speak-buddi-be/repositories/content_repo.py | sửa | `soft_delete_topic`, `soft_delete_word` (UPDATE is_active=FALSE) |
| speak-buddi-be/repositories/quiz_repo.py | sửa | `soft_delete_test` trả `bool` (rowcount) |
| speak-buddi-be/routers/admin_content.py | sửa | DELETE `/api/admin/topics|words|tests/{id}` → 204 |
| speak-buddi/src/features/admin/services/adminContentService.js | sửa | `apiDelete` + `disableTopic/Word/Test`; `listTests` hỗ trợ `includeInactive` |
| speak-buddi/src/features/admin/components/ConfirmDisableModal.jsx | thêm | Modal xác nhận vô hiệu hóa (AC-13-03) |
| speak-buddi/src/features/admin/topics/AdminTopicsPage.jsx | sửa | Filter Status All/Active/Draft + nút Vô hiệu hóa |
| speak-buddi/src/features/admin/vocabulary/AdminVocabularyPage.jsx | sửa | Filter Status + cột Status + disable action |
| speak-buddi/src/features/admin/tests/AdminTestsPage.jsx | sửa | Filter Status + badge Draft + disable |
| speak-buddi/src/features/admin/tests/AdminTestEditorPage.jsx | sửa | Nút Vô hiệu hóa trên header (edit mode) |

## Mockup UI đã tham chiếu

- `speak-buddi-docs/ui/quan_li_chu_de_admin/` — filter Status: All/Active/Draft, badge Draft

## Đối chiếu Acceptance Criteria

- [x] **AC-13-03** — Admin disable topic/word/test qua ConfirmDisableModal → DELETE API → `is_active=false`
- [x] **§5.3** — Soft delete only (UPDATE), không hard delete, không đổi schema
- [x] **Học viên ẩn nội dung** — read-side đã filter `is_active=TRUE` (learning/quiz/roadmap — không sửa, tái dùng S3/S4)
- [x] **Admin thấy inactive** — filter Status + badge Active/Draft trên list

## Lệnh đã chạy & kết quả

- `npm run lint` → 0 lỗi trong `features/admin/**`; 7 lỗi pre-existing RoadmapSection/RoadmapNode
- `python -m py_compile` → pass (content_repo, quiz_repo, admin_content)

## Cách test thủ công

1. Login admin → `/admin/topics` → chọn topic active → Vô hiệu hóa → confirm
2. Filter Status: Draft → thấy topic vừa disable
3. Login student → `/vocabulary` — topic inactive không hiện
4. Lặp từ vựng (`/admin/vocabulary`) và test (`/admin/tests`, editor)
5. `curl -X DELETE -H "Authorization: Bearer <student>" .../api/admin/topics/{id}` → 403

## Ngoài phạm vi / nợ kỹ thuật / TODO

- Re-enable (`is_active=true`) → **S9.5**
- Smoke test runtime PostgreSQL chưa chạy trong session
- Disable topic không cascade words/tests con (theo plan)
