# Implement — S10.2: Soft delete payment plan

- **Story:** S10.2
- **Epic:** EPIC-10 — Admin: Gói thanh toán
- **Ngày implement:** 2026-06-07
- **Verdict:** Hoàn thành
- **UI mockup tham chiếu:** `speak-buddi-docs/ui/quan_li_goi_thanh_toan_admin/` (layout card S10.1); pattern disable/filter bám `AdminTopicsPage` (S9.2)

## Tóm tắt

Bổ sung soft delete gói thanh toán: `DELETE /api/admin/payment-plans/{id}` → `is_active=false`; FE nút **Vô hiệu hóa** + `ConfirmDisableModal` + filter trạng thái trên `/admin/payments`. Read-side public (`payment_repo.list_active_plans`, `get_active_plan`) giữ filter `is_active=TRUE` — không sửa.

## File đã tạo / sửa

### Backend (`speak-buddi-be/`)

| File | Thay đổi |
|---|---|
| `repositories/payment_plan_admin_repo.py` | Thêm `soft_delete_plan()` — UPDATE `is_active=FALSE`, không commit trong repo |
| `routers/admin_payment_plan.py` | Thêm `DELETE /api/admin/payment-plans/{id}` → 204/404 + log |

### Frontend (`speak-buddi/`)

| File | Thay đổi |
|---|---|
| `payment-plans/services/paymentPlanService.js` | Thêm `disablePlan(id)` + helper `apiDelete` |
| `payment-plans/components/PaymentPlanCard.jsx` | Nút "Vô hiệu hóa" + prop `onDisable` |
| `payment-plans/AdminPaymentPlansPage.jsx` | Filter Status, `ConfirmDisableModal`, wire disable flow |

## Lint / test

| Kiểm tra | Kết quả |
|---|---|
| ESLint `src/features/admin/payment-plans` | PASS — 0 lỗi |
| BE `py_compile` | PASS — `payment_plan_admin_repo.py`, `admin_payment_plan.py` |
| Read-side verify (code) | PASS — `payment_repo.py` WHERE `is_active = TRUE` cho list/checkout |
| Smoke PostgreSQL E2E | Chưa chạy |

## AC checklist

| AC | Trạng thái | Ghi chú |
|---|---|---|
| AC-14-03 — disable + confirm → is_active=false | ✅ Code | DELETE + modal + badge "Đã tắt" |
| §5.3 soft delete | ✅ Code | UPDATE only, không hard delete |
| Guest không thấy inactive | ✅ Code | `payment_repo` filter sẵn |
| Admin filter + badge | ✅ Code | Select All/Đang bán/Đã tắt |
| BR07 admin-only | ✅ Code | Router `require_admin` + `AdminRoute` |
| Re-enable UI | ⏭ Out of scope | Theo plan |

## Hướng verify cho Reviewer

1. Admin `/admin/payments` → Vô hiệu hóa gói test → confirm → badge "Đã tắt".
2. `GET /api/payment/plans` không còn `id` gói đó.
3. `POST /api/payment/checkout` với plan disabled → 404.
4. Filter "Đang bán" / "Đã tắt" hoạt động đúng.
5. Student `DELETE /api/admin/payment-plans/{id}` → 403.
