# Implement — S11.1: Dashboard doanh thu/user/learning/AI

- **Story:** S11.1
- **Epic:** EPIC-11 — Admin: Phân tích & Báo cáo
- **Ngày implement:** 2026-06-07
- **Verdict:** Hoàn thành
- **UI mockup tham chiếu:** `speak-buddi-docs/ui/dashboard_quan_tri_desktop/` (`code.html` + `screen.png`), `speak-buddi-docs/ui/speak_buddi/DESIGN.md`

## Tóm tắt

Hoàn thiện dashboard admin `/admin/dashboard`: BE analytics đã có sẵn (overview + timeseries); bổ sung FE trang dashboard với 4 KPI cards, dải chỉ số học tập, biểu đồ recharts (user/revenue), top từ vựng và card AI usage placeholder. Redirect `/admin` → `/admin/dashboard`.

## Quyết định implement

- **Doanh thu:** Giữ `is_estimated=true` từ BE (subscription × price); hiển thị hint trên KPI card.
- **AI usage:** Hiển thị "Sắp có" khi `ai_usage.is_available=false` (Epic 7 chưa có bảng).
- **Trend badge:** Không hiển thị % xu hướng giả (StatCard không truyền `trend`) — tránh số liệu bịa.
- **Mockup "Latest Content":** Bỏ, thay bằng TopWordsCard + AiUsageCard theo plan.

## File đã tạo / sửa

### Backend (`speak-buddi-be/`)

| File | Thay đổi |
|---|---|
| `schemas/analytics.py` | ✅ Có sẵn — không sửa |
| `repositories/analytics_repo.py` | ✅ Có sẵn — không sửa |
| `routers/admin_analytics.py` | ✅ Có sẵn — không sửa |
| `main.py` | ✅ Router đã wire |

### Frontend (`speak-buddi/`)

| File | Thay đổi |
|---|---|
| `src/features/admin/AdminDashboardPage.jsx` | **Mới** — trang dashboard chính |
| `src/features/admin/components/UserActivityChart.jsx` | **Mới** — recharts + metric/range selects |
| `src/features/admin/components/TopWordsCard.jsx` | **Mới** — top 10 từ học nhiều |
| `src/features/admin/components/AiUsageCard.jsx` | **Mới** — AI placeholder / stats |
| `src/features/admin/components/AdminLayout.jsx` | Title "Dashboard" cho `/admin/dashboard` |
| `src/features/admin/components/AdminSidebar.jsx` | Chỉ highlight Overview (không double-active Analytics) |
| `src/app/App.jsx` | Route `/admin/dashboard`; `/admin` → dashboard |

## Lint / test

| Kiểm tra | Kết quả |
|---|---|
| ESLint (file S11.1) | PASS — 0 lỗi trên 7 file đã sửa |
| BE smoke E2E PostgreSQL | Chưa chạy — cần DB + token admin khi review |
| FE admin login E2E | Chưa chạy — cần runtime khi review |

## AC checklist

| AC | Trạng thái | Ghi chú |
|---|---|---|
| AC-12-01 — revenue/user/learning/AI metrics | ✅ Code | 4 nhóm trên UI + API overview |
| §3.6 overview (user free/paid, new users, revenue, quiz, accuracy, top words, AI duration) | ✅ Code | AI duration = placeholder; còn lại từ API |
| Admin-only (`AdminRoute`) | ✅ Code | Tái dùng guard sẵn có |
| Zero/empty state | ✅ Code | 0 / "Chưa có dữ liệu" / "Sắp có" |
| NFR §4.1 API < 1s | ⚠️ Chưa đo | Query repo đã tách, không N+1 — cần smoke trên DB thật |

## Phụ thuộc / ngoài phạm vi

- **S11.2:** Lọc doanh thu theo ngày/tháng/năm/gói — dropdown chart đã có `range`/`metric` làm nền.
- **S11.3:** Export Excel/PDF — nav Reports vẫn placeholder `/admin/reports`.
- **S7.x:** Khi có bảng AI usage, bổ sung query `get_ai_usage_stats` — không đổi contract FE.
