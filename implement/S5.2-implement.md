# Implement Log — S5.2: Lưu lịch sử dịch (translation_history)

- **Ngày:** 2026-06-07
- **Plan tham chiếu:** plan/S5.2-plan.md
- **Kết quả:** Hoàn thành
- **Màn UI tham chiếu:** `speak-buddi-docs/ui/dich_thuat_desktop/code.html` — section `<!-- History Section -->` (dòng 273–306): grid 2 cột, badge EN→VI, source/target text line-clamp-1, hover copy button.

---

## Thay đổi theo file

| File | Loại | Mô tả |
|---|---|---|
| `speak-buddi-be/db/migrations/006_translation_history.sql` | tạo mới | Migration SQL tạo bảng `translation_history` (UUID PK, user_id FK CASCADE, source_text, target_text, created_at TIMESTAMPTZ) + index `(user_id, created_at DESC)` |
| `speak-buddi-be/repositories/translate_repo.py` | tạo mới | Repository: `save_history(db, user_id, source_text, target_text)` + `get_history(db, user_id, limit=20)` — raw SQL + `sqlalchemy.text`, pattern bám quiz_repo.py |
| `speak-buddi-be/schemas/translate.py` | sửa | Thêm `TranslationHistoryItem` (id, source_text, target_text, created_at: datetime) + `TranslationHistoryResponse(items: list)` |
| `speak-buddi-be/routers/translate.py` | sửa | Đổi `def translate` → `async def translate`, inject `db: AsyncSession = Depends(get_db)`, wrap `translate_text` bằng `asyncio.to_thread`, gọi `save_history` trong try/except sau dịch thành công; thêm `GET /api/translate/history` endpoint |
| `speak-buddi/src/features/translate/services/translateService.js` | sửa | Thêm `getTranslationHistory()` gọi GET /api/translate/history, trả `data.items` |
| `speak-buddi/src/features/translate/components/TranslationHistory.jsx` | tạo mới | Component section lịch sử: section header + grid 2 cột (desktop) / 1 cột (mobile), card với badge EN→VI + timestamp + source/target text (line-clamp-1) + copy button; loading skeleton; empty state; lỗi API ẩn lặng |
| `speak-buddi/src/features/translate/components/TranslatorCard.jsx` | sửa | Nhận prop `onTranslated` (optional callback), gọi `onTranslated?.()` sau khi `setTranslation(result)` thành công |
| `speak-buddi/src/features/translate/TranslatePage.jsx` | sửa | Import `useAuth` + `TranslationHistory`, thêm state `historyTrigger`, truyền `onTranslated={handleTranslated}` xuống `TranslatorCard`, render `<TranslationHistory refreshTrigger={historyTrigger} />` chỉ khi `isAuthenticated` |

---

## Đối chiếu Acceptance Criteria

- [x] **AC-07-03** — Mỗi lần dịch thành công (user đã login): BE lưu vào `translation_history` qua `save_history()` (try/except đảm bảo lỗi DB không fail kết quả dịch). FE gọi `GET /api/translate/history` sau mỗi lần dịch, hiển thị 20 mục mới nhất trong section lịch sử.
- [x] Guest (chưa login): `TranslationHistory` không render (`isAuthenticated === false`), endpoint `/history` trả 401 (Depends(current_user) tự xử lý).
- [x] Lỗi DB `save_history` không làm fail kết quả dịch — `try/except` trong router, log error, không raise.
- [x] Grid 2 cột desktop / 1 cột mobile theo mockup `dich_thuat_desktop/code.html`.
- [x] Card: badge EN→VI, source text (line-clamp-1), target text (line-clamp-1), timestamp dd/MM HH:mm, copy button on-hover.
- [x] Loading skeleton khi đang fetch lịch sử.
- [x] Empty state: "Chưa có lịch sử dịch." khi chưa có bản dịch nào.
- [x] Migration SQL `006_translation_history.sql` với index tối ưu query.

---

## Lệnh đã chạy & kết quả

- `python -m py_compile repositories/translate_repo.py schemas/translate.py routers/translate.py` → **OK** (không lỗi syntax)
- `npm run lint` (trong `speak-buddi/`) → **7 lỗi pre-existing** trong `RoadmapSection.jsx` (5 lỗi: unused vars + conditional hook) và `RoadmapNode.jsx` (2 lỗi: unused vars). **Không có lỗi mới** từ các file S5.2 đã tạo/sửa.

---

## Cách test thủ công

1. Chạy migration: `psql -U <user> -d speakbuddi -f speak-buddi-be/db/migrations/006_translation_history.sql`
2. Khởi động BE: `cd speak-buddi-be && uvicorn main:app --reload --port 8000`
3. Khởi động FE: `cd speak-buddi && npm run dev`
4. Đăng nhập → vào trang `/translate`
5. Nhập "Hello" → nhấn Dịch → section "Lịch sử dịch gần đây" xuất hiện phía dưới với card đầu tiên (badge EN→VI, text "Hello", bản dịch tiếng Việt, timestamp hiện tại)
6. Dịch thêm 2–3 câu → list cập nhật, mục mới nhất trên đầu
7. Hover vào card → nút copy hiện lên → click → bản dịch được copy vào clipboard
8. Mở trang trong trạng thái guest (chưa đăng nhập) → section lịch sử không hiển thị
9. Đăng nhập lại → lịch sử cũ vẫn còn (đã lưu DB)
10. Kiểm tra responsive 375px → history grid 1 cột

---

## Ngoài phạm vi / nợ kỹ thuật / TODO

- Nút "Clear all" trong mockup: nằm ngoài scope S5.2 (ghi chú trong plan là P3). Không implement.
- Pre-existing lint errors trong `RoadmapSection.jsx` và `RoadmapNode.jsx`: ngoài scope S5.2, cần fix riêng.
- Migration chưa được chạy tự động — cần DBA/dev chạy thủ công trước khi deploy.
- Pagination lịch sử (>20 mục): chưa có — hiện tại chỉ trả top 20 (đủ theo AC-07-03).
- `source_lang`/`target_lang` hardcode EN→VI: phù hợp S5.1 (chỉ EN→VI). Nếu sau này support đa chiều dịch, cần thêm cột vào DB.
