# Implement Log — S1.9: Đăng xuất — xóa JWT client-side + nút logout UI (user + admin)

**Story:** S1.9 · **Epic:** EPIC-1  
**Ngày:** 2026-06-07  
**Verdict:** Hoàn thành — chuyển sang `review`

---

## UI tham chiếu

- `speak-buddi-docs/ui/lo_trinh_hoc_tap_snake_style/code.html` — footer "Sign Out" (icon logout + hover error tone)
- `speak-buddi-docs/ui/luyen_phat_am_desktop/code.html` — sidebar footer logout
- `speak-buddi-docs/ui/hoi_thoai_ai_desktop/code.html` — sidebar footer logout
- Admin: tái dùng pattern user sidebar (`dashboard_quan_tri_desktop` bottom zone)

---

## Tóm tắt thay đổi

### Frontend — `speak-buddi/src/`

| Task | File | Thay đổi |
|---|---|---|
| FE-1 | `shared/auth/useLogout.js` | Tạo mới — hook gọi `AuthContext.logout()` + `navigate("/login", { replace: true })` |
| FE-2 | `shared/components/LogoutButton.jsx` | Tạo mới — nút "Đăng xuất" với `LuLogOut`, hover error-container, min-height 44px |
| FE-3 | `features/dashboard/components/DashSidebar.jsx` | Thêm divider + `<LogoutButton variant="sidebar-user" />` ở footer sidebar |
| FE-4 | `features/admin/components/AdminSidebar.jsx` | Thêm `<LogoutButton variant="sidebar-admin" />` dưới "Cài đặt" |
| FE-5 | `shared/api/client.js` | `dispatchAuthChanged()` sau mỗi `clearTokens()` khi 401 (đồng bộ AuthContext) |
| FE-6 | `shared/auth/authService.js` | Xóa export `logout()` deprecated (không còn caller) |

### Backend

Không thay đổi (JWT stateless v1.1 — logout client-side theo plan).

---

## Files thay đổi

```
speak-buddi/src/shared/auth/useLogout.js                          (tạo mới)
speak-buddi/src/shared/components/LogoutButton.jsx                (tạo mới)
speak-buddi/src/features/dashboard/components/DashSidebar.jsx     (sửa)
speak-buddi/src/features/admin/components/AdminSidebar.jsx       (sửa)
speak-buddi/src/shared/api/client.js                               (sửa)
speak-buddi/src/shared/auth/authService.js                         (sửa)
```

---

## AC mapping

| AC | Trạng thái | Ghi chú |
|---|---|---|
| AC-S1.9-01 | ✅ | `logout()` → `clearTokens()` xóa `token`, `refresh_token`, `user_info` |
| AC-S1.9-02 | ✅ | `useLogout` redirect `/login` |
| AC-S1.9-03 | ✅ | `ProtectedRoute` dựa `isAuthenticated` — false sau logout |
| AC-S1.9-04 | ✅ | Admin sidebar logout + `AdminRoute` chặn `/admin/*` |
| AC-S1.9-05 | ✅ | `dispatchAuthChanged` + `storage` event (AuthContext sẵn có) |
| AC-S1.9-06 | ✅ | Icon + label "Đăng xuất", footer sidebar, hover error tone |
| AC-S1.9-07 | ✅ | `type="button"`, `aria-label`, min-height 44px, `:focus-visible` |
| AC-S1.9-08 | ✅ | Không log token |
| AC-S1.9-09 | ✅ | `DeleteAccountSection` không đổi — vẫn `logout()` + `navigate("/")` |

---

## Lint / test

- `npm run lint`: **9 errors** trong file khác (ConversationPage, RoadmapSection, RoadmapNode) — **không phát sinh từ S1.9**; file S1.9 không có lint error.
- Verify thủ công: login → bấm Đăng xuất (sidebar user/admin) → localStorage sạch → `/login`.

---

## TBD / ghi chú cho Reviewer

- **Mobile:** sidebar ẩn ≤768px — chưa có entry logout trên mobile (Profile/mobile menu) — ghi nhận trong plan §6, không chặn story.
- **Google GSI:** chưa gọi `disableAutoSelect()` sau logout — optional.
