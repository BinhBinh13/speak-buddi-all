# Implement — S12.3: Form liên hệ / hỗ trợ + support email

- **Ngày:** 2026-06-07
- **Trạng thái:** Hoàn thành (sẵn sàng review)

## Mockup / UI tham chiếu

- `speak-buddi-docs/ui/landing_page_desktop` — footer links
- `speak-buddi-docs/ui/pricing_page_desktop` — footer links
- `speak-buddi-docs/ui/thanh_toan_that_bai_desktop` — support CTA
- Shell: `PricingPage.jsx`, `ForgotPasswordPage.jsx`, `DESIGN.md`

## File thay đổi

### Backend (`speak-buddi-be/`)

| File | Hành động | Mô tả |
|---|---|---|
| `schemas/support.py` | mới | `ContactRequest`, `ContactResponse`, `SUBJECT_CHOICES` / labels |
| `routers/support.py` | mới | `POST /api/support/contact` — public, honeypot, validation §5.2, `optional_current_user` |
| `services/email_service.py` | sửa | `send_support_contact_email()` — To/From = `SMTP_USER`, Reply-To = email user |
| `main.py` | sửa | `include_router(support.router)` |

### Frontend (`speak-buddi/`)

| File | Hành động | Mô tả |
|---|---|---|
| `src/features/support/ContactPage.jsx` | mới | Form công khai `/contact`, success state, honeypot, pre-fill auth |
| `src/features/support/services/supportService.js` | mới | `submitContact()` → POST API |
| `src/app/App.jsx` | sửa | Route `/contact` |
| `src/shared/components/PublicFooter.jsx` | sửa | "Liên hệ"/"Trợ giúp" → `/contact` |
| `src/features/payment/PaymentResultPage.jsx` | sửa | Link hỗ trợ → `/contact` |
| `src/features/legal/PrivacyPolicyPage.jsx` | sửa | Mục 8 thêm link form liên hệ |

## AC mapping

| AC | Trạng thái | Ghi chú |
|---|---|---|
| AC-S12.3-1 | ✅ | Route `/contact`, 4 trường + select chủ đề |
| AC-S12.3-2 | ✅ | Email gửi tới `SMTP_USER` (inbox SMTP hiện tại) |
| AC-S12.3-3 | ✅ | BE validation tiếng Việt + max length |
| AC-S12.3-4 | ✅ | UI tokens, nút minHeight 44px, `role="alert"` |
| AC-S12.3-5 | ✅ | Log chỉ `SUPPORT_CONTACT ok/skip/honeypot` — không log nội dung |
| AC-S12.3-6 | ✅ | PublicFooter cập nhật |
| AC-S12.3-7 | ✅ | Privacy + PaymentResultPage |
| AC-S12.3-8 | ✅ | Pre-fill name/email từ `useAuth()` |
| AC-S12.3-9 | ✅ | Success box ẩn form |

## Kiểm chứng đã chạy

- `python -m py_compile` — pass (support.py, email_service.py, main.py)
- `npx eslint` (6 file FE story) — 0 lỗi

## Chưa verify / nợ nhỏ

- Chưa gửi email E2E thật trong session (cần BE chạy + SMTP configured)
- Không rate limit / captcha (đã ghi trong plan)
- Privacy Policy vẫn hiển thị `mailto:support@speakbuddi.vn` placeholder — form `/contact` là kênh chính; inbox thật = `SMTP_USER` (không expose FE)

## Quyết định PO đã áp dụng

- Inbox support = **`SMTP_USER`** — không env `SUPPORT_EMAIL` riêng
