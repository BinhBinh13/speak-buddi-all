# Implement Log — S12.2: Người dùng yêu cầu xóa dữ liệu cá nhân

- **Ngày:** 2026-06-07
- **Plan tham chiếu:** plan/S12.2-plan.md
- **Kết quả:** Hoàn thành

## Tham chiếu UI
Mockup **Danger Zone** trong `speak-buddi-docs/ui/ho_so_cai_dat_desktop/code.html` (dòng ~412–423): khối nền error-container, tiêu đề xóa tài khoản, mô tả không thể hoàn tác, nút outline đỏ. Port sang tiếng Việt trên `/profile` theo DESIGN.md (error `#ba1a1a`, Be Vietnam Pro).

## Thay đổi theo file

| File | Loại | Mô tả |
|---|---|---|
| `speak-buddi-be/schemas/profile.py` | sửa | Thêm `DeleteAccountRequest`, `DeleteAccountOut`. |
| `speak-buddi-be/schemas/auth.py` | sửa | Thêm field `has_password: bool` vào `user_to_dict()` (FE biết có cần nhập mật khẩu khi xóa). |
| `speak-buddi-be/repositories/user_repo.py` | sửa | Thêm `delete_user_personal_data()` — transaction xóa PII học tập + anonymize user (`status=deleted`, email `deleted+<uuid>@speakbuddi.invalid`); giữ `payment_transaction`. |
| `speak-buddi-be/routers/profile.py` | sửa | Thêm `DELETE /api/profile/account` — validate `confirm_text=XOA`, password nếu có hash, chặn admin 403, log `ACCOUNT_DELETE ok`. |
| `speak-buddi/src/features/profile/components/DeleteAccountSection.jsx` | thêm | Section "Vùng nguy hiểm" + wire modal + logout/navigate sau success. |
| `speak-buddi/src/features/profile/components/ConfirmDeleteAccountModal.jsx` | thêm | Modal xác nhận: bullet dữ liệu xóa, input `XOA`, password (nếu `has_password`), lỗi inline. |
| `speak-buddi/src/features/profile/services/profileService.js` | sửa | Thêm `deleteAccount()` → `DELETE /api/profile/account`. |
| `speak-buddi/src/features/profile/ProfilePage.jsx` | sửa | Render `DeleteAccountSection`; nav link `#danger-zone`. |
| `speak-buddi/src/features/legal/PrivacyPolicyPage.jsx` | sửa | Mục 6 trỏ luồng in-app `/profile` (AC-S12.2-9) + giữ email dự phòng. |

## Đối chiếu Acceptance Criteria

- [x] AC-S12.2-1 — UI xóa trên `/profile` (DeleteAccountSection) + API `DELETE /api/profile/account` yêu cầu JWT.
- [x] AC-S12.2-2 — Modal xác nhận; bắt buộc gõ `XOA`; password bắt buộc nếu `password_hash` có (OAuth-only bỏ qua).
- [x] AC-S12.2-3 — Repo xóa: translation_history, user_word_progress, quiz_attempt, oauth_account, user_session, password_reset_token, user_voice_preference; clear user_profile PII.
- [x] AC-S12.2-4 — `users.status=deleted`, email anonymized, password_hash=NULL; login lọc `status != 'deleted'`.
- [x] AC-S12.2-5 — Success: `logout()`, `sessionStorage.auth_msg`, navigate `/`.
- [x] AC-S12.2-6 — UI tiếng Việt, Danger Zone mockup, touch target ≥44px, lỗi text đỏ có `role="alert"`.
- [x] AC-S12.2-7 — Admin → 403 với message hướng dẫn liên hệ nội bộ.
- [x] AC-S12.2-8 — Log chỉ `ACCOUNT_DELETE ok user=<id>`; không log password/confirm_text.
- [x] AC-S12.2-9 — PrivacyPolicyPage mục 6 cập nhật trỏ `/profile`.

## Lệnh đã chạy & kết quả

- `python -m py_compile routers/profile.py repositories/user_repo.py schemas/profile.py schemas/auth.py` → **pass**
- `npx eslint src/features/profile/** src/features/legal/PrivacyPolicyPage.jsx` (5 file story) → **pass**

## Cách test thủ công

1. Đăng ký user test → tạo vài dữ liệu (học từ / quiz / dịch).
2. Vào `/profile` → cuộn **Vùng nguy hiểm** → **Xóa tài khoản**.
3. Modal: nhập `XOA` + mật khẩu (nếu có) → **Xóa vĩnh viễn** → redirect `/`, đã logout.
4. Thử đăng nhập lại email cũ → thất bại.
5. (PostgreSQL) Kiểm tra `users.status='deleted'`, email `deleted+...@speakbuddi.invalid`; count progress/quiz/translation = 0.
6. Đăng nhập admin → thử xóa → 403.

## Ngoài phạm vi / nợ kỹ thuật

- Bảng pronunciation audio / AI transcript (Epic 6/7) chưa có schema — chưa DELETE; ghi chú mở rộng sau.
- Smoke PostgreSQL E2E chưa chạy trong session implement.
- Session cũ trên FE có thể thiếu `has_password` → FE mặc định yêu cầu password (an toàn hơn); re-login/`/me` sẽ có field đúng.
