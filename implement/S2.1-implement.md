# Implement Log — S2.1: Form onboarding + chọn level A1–C2 + validation bắt buộc (REVISED v2 — Scenario-based AI Roadmap)

- **Ngày:** 2026-06-11
- **Plan tham chiếu:** plan/S2.1-plan.md
- **Kết quả:** Hoàn thành

## Màn UI tham chiếu
- `onboarding_chon_so_thich_desktop` — card grid pattern (responsive 2-3-4 col), chip-selected/chip-unselected style, icon + label + description, border-2 rounded-xl, check mark top-right. Đã port sang React + inline style theo DESIGN.md (primary #3525cd, Be Vietnam Pro, surface tokens). GoalStep dùng 3 card cố định (single-select) thay vì multi-select chip.
- `onboarding_chon_trinh_do_desktop` — layout tổng thể OnboardingPage (đã tham chiếu).
- `onboarding_chon_muc_tieu_desktop` — card option pattern (đã tham chiếu).

## Thay đổi theo file

| File | Loại | Mô tả |
|---|---|---|
| `speak-buddi-be/db/migrations/012_roadmap_sequence.sql` | thêm | Migration idempotent: ALTER TABLE user_profile ADD COLUMN IF NOT EXISTS roadmap_sequence JSONB |
| `speak-buddi-be/db/schema_core.sql` | sửa | Thêm `roadmap_sequence JSONB` vào khối CREATE TABLE user_profile (env mới có sẵn cột) |
| `speak-buddi-be/schemas/onboarding.py` | sửa | OnboardingRequest: bỏ `topics`, thêm `learning_goal: str`. OnboardingOut: bỏ `topics`, thêm `learning_goal: str` + `roadmap_generated: bool` |
| `speak-buddi-be/schemas/roadmap.py` | sửa | RoadmapNode: thêm `scenario_name: str | None` và `scenario_description: str | None` (optional, backward-compat) |
| `speak-buddi-be/repositories/user_repo.py` | sửa | `update_onboarding`: bỏ param `topics`, thêm `learning_goal`; SET interests=NULL; RETURNING thêm learning_goal. Thêm `update_roadmap_sequence(db, user_id, sequence)` lưu JSONB |
| `speak-buddi-be/services/roadmap_ai_service.py` | thêm | Hàm `generate_roadmap_sequence(level, goal, topics)` — gọi Claude với prompt scenario-based, parse JSON array of objects, anti-hallucination (validate topic_id + append bỏ sót), backward-compat element string, degrade exception → return [] |
| `speak-buddi-be/routers/onboarding.py` | sửa | VALID_GOALS = {"travel","work","communication"}. POST /api/onboarding: validate goal → update_onboarding → fetch topics → gen sequence → save (degrade an toàn). GET /topics giữ lại với note DEPRECATED |
| `speak-buddi-be/repositories/roadmap_repo.py` | sửa | `get_profile_for_roadmap`: SELECT thêm roadmap_sequence. `get_roadmap`: nếu sequence != NULL → `_reorder_by_sequence` (gắn scenario_name/description + append topic bỏ sót); NULL → fallback sort difficulty + scenario=None. Thêm helper `_reorder_by_sequence` |
| `speak-buddi/src/features/onboarding/components/GoalStep.jsx` | thêm | 3 radio card cố định (travel/work/communication). Single-select, props `{value, onChange}`. Bám mockup onboarding_chon_so_thich_desktop: card p-6 rounded-xl border-2, icon + label + description, check mark top-right, responsive 1col mobile / 3col desktop, touch target >= 44px |
| `speak-buddi/src/features/onboarding/components/TopicStep.jsx` | xóa | Không còn dùng (thay bằng GoalStep) |
| `speak-buddi/src/features/onboarding/services/onboardingService.js` | sửa | Xóa `getTopics()`. Cập nhật JSDoc `submitOnboarding`: payload field `learning_goal: string` thay `topics` |
| `speak-buddi/src/features/onboarding/OnboardingPage.jsx` | sửa | Bỏ import getTopics/TopicStep/useEffect; import GoalStep. Bỏ state topics/topicList/loadingTopics/topicError/retryCount. Thêm `[goal, setGoal] = useState("")`. canContinue step 2: `goal !== ""`. Render step 2: GoalStep. Submit payload: `{level, learning_goal: goal, ...}`. updatedUser thêm learning_goal |

## Đối chiếu Acceptance Criteria

- [x] AC-03-01 — Guard redirect onboarding cho user `onboarding_completed=false` không bị đụng; code guard đã tồn tại và không thay đổi.
- [x] AC-03-02 — Hoàn tất 4 bước: lưu level + learning_goal vào user_profile, AI sinh roadmap_sequence dạng scenario `{topic_id, scenario_name, scenario_description}` và lưu JSONB. GET /api/roadmap trả nodes đúng thứ tự sequence + có scenario_name/description.
- [x] AC-03-03 — Bước 2 chưa chọn goal → `canContinue()` = false → nút disabled, không POST.
- [x] BR09 — Level validate FE (LevelStep chỉ A1–C2) + BE (VALID_LEVELS check → 400).
- [x] Backward-compat — user cũ roadmap_sequence=NULL → fallback sort difficulty, scenario=None.
- [x] Goal validation BE — `learning_goal` ngoài VALID_GOALS → HTTP 400.
- [x] AI degrade — exception → return []; onboarding 200, roadmap_generated=False.

## Lệnh đã chạy & kết quả

- `npx eslint src/features/onboarding/` → **pass** (0 errors, 0 warnings)
- `npm run lint` toàn project → 17 issues, **tất cả pre-existing** ở file ngoài phạm vi S2.1 (AdminPaymentPlansPage, ConversationPage, RoadmapSection, PaymentResultPage, PersonalInfoSection, PronunciationPage, SpeakingPage). Không có lỗi nào từ code S2.1.

## Cách test thủ công

1. **Chạy migration:** `psql -U postgres -d speakbuddi -f speak-buddi-be/db/migrations/012_roadmap_sequence.sql`
2. **AC-03-02:** Đăng ký user mới → hoàn tất 4 bước với goal=travel → `SELECT target_level, learning_goal, roadmap_sequence FROM user_profile` → có level, goal="travel", mảng JSONB object `{topic_id, scenario_name, scenario_description}`. GET /api/roadmap → nodes đúng thứ tự, có scenario fields.
3. **AC-03-03:** Vào bước 2 GoalStep chưa chọn → nút "Tiếp tục" disabled; kiểm tra Network tab không có POST.
4. **BR09:** `POST /api/onboarding {"level":"X1",...}` → 400.
5. **Goal validation:** `POST /api/onboarding {"learning_goal":"foo",...}` → 400.
6. **AI degrade:** Set ANTHROPIC_API_KEY=invalid → submit → response 200 `roadmap_generated=false`; GET /api/roadmap vẫn trả nodes (fallback).
7. **Backward-compat:** User cũ roadmap_sequence=NULL → GET /api/roadmap → nodes sort difficulty, scenario_name/description=null, không lỗi.
8. **Responsive GoalStep:** 375px → 1 col; 768px+ → 3 col; mỗi card ≥ 44px height.

## Ngoài phạm vi / nợ kỹ thuật / TODO

- Lint lỗi pre-existing ở AdminPaymentPlansPage, ConversationPage, PaymentResultPage, PersonalInfoSection, RoadmapSection, PronunciationPage, SpeakingPage — ngoài phạm vi S2.1.
- **5-node test gate** (học xong 5 node → test AI conversation + MC): TBD story mới EPIC-4. `scenario_name` trong roadmap_sequence đã sẵn sàng cho test gate.
- `selected_topics` trong RoadmapOut luôn = 0 (interests=NULL) — S2.4 có thể cần ẩn chỉ số này. Ghi nhận cho reviewer.
- AI latency (~2-5s) ở bước cuối onboarding chấp nhận được (chỉ 1 lần). Nếu cần nhanh → gen async/background (plan §6, TBD).
