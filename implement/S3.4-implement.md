# Implement Log — S3.4: Hiển thị nội dung crawl mới, giữ tiến độ cũ

- **Ngày:** 2026-06-06
- **Plan tham chiếu:** plan/S3.4-plan.md
- **Kết quả:** Hoàn thành
- **Màn UI tham chiếu:** `speak-buddi-docs/ui/hoc_tu_vung_desktop/` (badge chip nhỏ cạnh tag badge hiện có)

## Thay đổi theo file

| File | Loại | Mô tả |
|---|---|---|
| `speak-buddi/src/features/vocabulary/components/Flashcard.jsx` | sửa | Thêm component `NewWordBadge` (badge "Mới" — secondary emerald, pill, 11px, font-weight 600); render cạnh `ProgressBadge` trong top row khi `word.source === 'langeek'` và `progressStatus == null` |
| `speak-buddi-be/db/verify_s3_4.sql` | thêm | Script SQL 4 kịch bản (A–D) giả lập crawler Langeek để Reviewer verify AC-05-04 a–d; idempotent |

## Đối chiếu Acceptance Criteria

- [x] **AC-05-04 (a)** — Mở topic sau khi crawler thêm từ mới: endpoint `GET /api/topics/{id}/words` đã `WHERE tw.is_active = TRUE` (S3.2); từ mới Langeek với `is_active=true` tự động xuất hiện trong response. `handleSelectTopic` gọi `getWords` mỗi lần mở topic (không cache) → FE render đầy đủ. Verify: kịch bản A trong `verify_s3_4.sql`.

- [x] **AC-05-04 (b)** — Các từ cũ giữ progress: `handleSelectTopic` gọi `getTopicProgress` mỗi lần mở (reset `progressMap` rồi hydrate lại từ API); endpoint đã JOIN `is_active=TRUE` nên chỉ trả progress của từ active; progressMap key = `topic_word_id` nên từ cũ vẫn active khớp đúng. Verify: kịch bản B trong `verify_s3_4.sql`.

- [x] **AC-05-04 (c)** — Từ soft-disabled biến mất UI nhưng progress vẫn trong DB: crawler/admin chạy `UPDATE topic_word SET is_active=FALSE` (soft-delete) → row `topic_word` vẫn tồn tại → FK `ON DELETE CASCADE` không kích → `user_word_progress` row được bảo toàn. Endpoint words đã filter `is_active=TRUE` → từ disabled không xuất hiện UI. Verify: kịch bản C trong `verify_s3_4.sql` (SELECT xác nhận row progress còn nguyên).

- [x] **AC-05-04 (d)** — `percent_known` re-base đúng: endpoint `GET /api/topics/{id}/progress` đã JOIN `topic_word tw WHERE tw.is_active=TRUE`, COUNT aggregate tính lại theo tập active hiện tại → khi có từ mới thêm hoặc từ disabled, `total_words` và `percent_known` tự cập nhật đúng mà không cần code thêm. Verify: kịch bản D trong `verify_s3_4.sql`.

- [x] **(UX) Badge "Mới"** — Component `NewWordBadge` thêm vào `Flashcard.jsx`: hiện khi `word.source === 'langeek'` **và** `progressStatus == null`; sau khi đánh dấu known/learning `progressStatus` không còn null → badge tự biến mất (optimistic update tức thì qua `setProgressMap` trong `handleKnow`/`handleNeedPractice`). Style bám DESIGN.md: secondary `#006c49`, bg `#e6f4ee`, font 11px, weight 600, radius 999px, padding `3px 10px`.

## Lệnh đã chạy & kết quả

- `npx eslint src/features/vocabulary/components/Flashcard.jsx src/features/vocabulary/VocabularyPage.jsx` → **pass** (0 lỗi trong file S3.4)
- `npm run lint` (toàn bộ FE) → fail 5 lỗi **pre-existing** trong `RoadmapSection.jsx` (không liên quan S3.4, ngoài scope)

## Cách test thủ công

1. Chạy DB: `psql -U postgres -d speakbuddi -f db/schema_core.sql -f db/schema_learning.sql -f db/schema_progress.sql -f db/seed_dev.sql`
2. Chạy verify script từng bước: `psql -U postgres -d speakbuddi -f db/verify_s3_4.sql`
3. Khởi động BE + FE, đăng nhập `demo@speakbuddi.com` / `password123`
4. Vào "Học từ vựng" → chọn level A1 → chọn topic "Greetings & Introductions"
5. **Verify A + Badge "Mới"**: từ `greet` (đã insert bởi verify script) phải hiện với chip "Mới" màu xanh lá
6. **Verify B**: các từ `goodbye` (learning) vẫn hiện badge "Cần luyện"; `hello` (known, disabled) không xuất hiện
7. **Verify C**: đếm số thẻ — chỉ 5 thẻ (hello đã soft-disable); kiểm DB `SELECT * FROM user_word_progress WHERE topic_word_id = (SELECT id FROM topic_word WHERE word='hello')` → row còn tồn tại
8. **Verify D**: progress bar phải tính trên 5 từ active; known=0, learning=1 (goodbye), percent=0%
9. Đánh dấu `greet` là known → badge "Mới" biến mất ngay lập tức

## Ràng buộc cho Epic 9 (S9.3 / S9.5) — BẮT BUỘC

> **Crawler Langeek và Admin PHẢI dùng soft-delete, CẤMSM hard-delete `topic_word`.**
>
> - **Đúng:** `UPDATE topic_word SET is_active = FALSE WHERE id = <id>` (soft-disable)
> - **SAI:** `DELETE FROM topic_word WHERE id = <id>` — sẽ kích `ON DELETE CASCADE` trên `user_word_progress`, **xóa toàn bộ lịch sử học** của mọi user đã học từ đó (vi phạm AC-05-04 c, BR11)
>
> Ràng buộc này đã được ghi chú trong `schema_progress.sql` (dòng 26–27) và cần được nhắc lại khi implement S9.2, S9.3, S9.5.

## Ngoài phạm vi / nợ kỹ thuật / TODO

- 5 lỗi lint pre-existing trong `RoadmapSection.jsx` (không thuộc S3.4 — cần sửa trong story riêng hoặc S9.1)
- (Optional) Đếm "X từ mới" ở header topic khi mở topic learn mode — nice-to-have, chưa implement
- Nếu sau này cần badge theo ngưỡng thời gian (ví dụ "crawl trong 7 ngày qua") thay vì "chưa học", cần thêm logic so sánh `word.created_at` — hiện dùng "chưa có progress" vì an toàn hơn và không phụ thuộc đồng hồ
- Crawler thật là S9.3 (chưa làm) — S3.4 chỉ verify bằng SQL giả lập (`verify_s3_4.sql`)
