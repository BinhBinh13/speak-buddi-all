# implement/

Log của command `/implement <story-id>`. Mỗi file `S<x>-implement.md` ghi lại Developer đã thay đổi gì (file, lệnh, đối chiếu AC). Code thật nằm trong `speak-buddi/` và `speak-buddi-be/`.
