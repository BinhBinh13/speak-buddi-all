# Implement — S9.5: Admin chỉnh/disable nội dung đã crawl + giữ lịch sử crawl

- **Story:** S9.5
- **Plan:** `plan/S9.5-plan.md`
- **Ngày:** 2026-06-07
- **Kết luận:** Hoàn thành (chờ review)

## UI tham chiếu

- `speak-buddi-docs/ui/quan_li_chu_de_admin/` — Topics (badge/filter Source, enable)
- `speak-buddi-docs/ui/quan_li_tu_vung_admin/` — Vocabulary (cột Source, lock icon)
- `speak-buddi-docs/ui/langeek_crawler_admin/` — Crawler config/history (verify-only, không đổi lớn)

## Thay đổi Backend (`speak-buddi-be/`)

| File | Mô tả |
|---|---|
| `db/schema_learning_s9_5.sql` | **Mới** — `admin_locked BOOLEAN` trên `topic`, `topic_word` |
| `repositories/content_repo.py` | Lock on admin disable/edit; `enable_topic/word`; crawler batch disable không lock; `upsert_langeek_*` skip locked |
| `repositories/quiz_repo.py` | `enable_test()` |
| `routers/admin_content.py` | `PATCH /topics\|words\|tests/{id}/enable` |
| `schemas/learning.py` | `admin_locked` trên TopicOut / TopicWordOut |
| `services/crawler_publish.py` | Stats `topics_skipped_locked`, `words_skipped_locked` + crawl log |

## Thay đổi Frontend (`speak-buddi/`)

| File | Mô tả |
|---|---|
| `services/adminContentService.js` | `enableTopic/Word/Test` + `apiPatch` |
| `components/ConfirmEnableModal.jsx` | **Mới** — modal kích hoạt lại |
| `components/ContentFormModal.jsx` | Cảnh báo khi sửa nội dung `source=langeek` |
| `topics/AdminTopicsPage.jsx` | Badge Langeek, filter Source, enable flow |
| `vocabulary/AdminVocabularyPage.jsx` | Cột Source, filter, lock icon, enable |
| `tests/AdminTestsPage.jsx` | Nút kích hoạt lại test inactive |
| `tests/AdminTestEditorPage.jsx` | Nút kích hoạt lại trên editor |

## Migration bắt buộc

```bash
psql -U postgres -d speakbuddi -f speak-buddi-be/db/schema_learning_s9_5.sql
```

Chạy trước khi test BE/FE trên PostgreSQL thật.

## AC mapping

| AC | Trạng thái | Ghi chú |
|---|---|---|
| AC-13-06 disable | ✅ Code | Admin disable → `is_active=false`, `admin_locked=true`; crawler skip UPDATE |
| AC-13-06 edit | ✅ Code | Admin PUT langeek → `admin_locked=true`; upsert skip |
| AC-13-06 config | ✅ Verify-only | `PUT /api/admin/crawler/config` đã có từ S9.3/S9.4 |
| BR11 learner | ✅ Regression | Read-side vẫn filter `is_active=TRUE` (S9.2) |
| Re-enable | ✅ Code | PATCH enable + FE modal |
| S3.4 progress | ✅ Design | Không xóa progress rows; chưa smoke E2E DB |
| Crawl history | ✅ Design | Không DELETE job/log khi disable |

## Verify thủ công (Reviewer)

1. Chạy migration S9.5.
2. Admin disable word `source=langeek` → chạy manual crawl (fixture) → word vẫn inactive.
3. Admin sửa `meaning_vi` → crawl lại → meaning giữ bản Admin.
4. PATCH enable → learner thấy lại nội dung.
5. `npm run lint` — 7 lỗi cũ ở `RoadmapSection.jsx` / `RoadmapNode.jsx` (ngoài phạm vi S9.5).

## TBD / ngoài scope

- Smoke PostgreSQL E2E chưa chạy trong session implement.
- Re-enable clear lock → crawler có thể sync lại field ở lần crawl kế (theo plan).
